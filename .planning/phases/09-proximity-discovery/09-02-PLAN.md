---
phase: 09-proximity-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/src/components/ClueBillboard.tsx, apps/mobile/src/components/ScanningProgress.tsx]
autonomous: true
requirements: [PLAY-04]

must_haves:
  truths:
    - "Clues display a hover label with their name when within 3.0 meters"
    - "Scanning progress ring is rendered as an animated SVG component"
    - "Clues visually transition (scale/glow) based on proximity states"
  artifacts:
    - path: "apps/mobile/src/components/ClueBillboard.tsx"
      provides: "Hover labels and detail reveal visuals"
    - path: "apps/mobile/src/components/ScanningProgress.tsx"
      provides: "HUD progress feedback"
---

<objective>
Enhance clue visuals with range-based detail reveal (hover labels) and implement the HUD progress indicator for discovery.

Purpose: To provide visual feedback that clues are being detected and "scanned."
Output: Updated `ClueBillboard.tsx` and new `ScanningProgress.tsx`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/components/ClueBillboard.tsx
@apps/mobile/src/components/LockingProgressRing.tsx
</context>

<tasks>

<task type="auto">
  <name>Implement Hover Labels in ClueBillboard</name>
  <files>apps/mobile/src/components/ClueBillboard.tsx</files>
  <action>
    Update ClueBillboard to handle detail reveal:
    - Add `cameraPosition` prop.
    - Calculate distance to camera inside the component (or pass `isNear` prop).
    - If `distance <= 3.0`:
      - Render a `ViroText` component 0.2m above the clue decal.
      - Display `clue.name` in the hover label.
    - Add scale-up animation/transition when entering the 3m range (D-04).
  </action>
  <verify>
    <automated>Verify ViroText is conditionally rendered in ClueBillboard.</automated>
  </verify>
  <done>Clues show labels and scale up when the player approaches within 3m.</done>
</task>

<task type="auto">
  <name>Create ScanningProgress component</name>
  <files>apps/mobile/src/components/ScanningProgress.tsx</files>
  <action>
    Create a new component based on `LockingProgressRing.tsx` pattern:
    - Use `react-native-svg` to draw a circular progress ring.
    - Props: `progress` (0 to 1), `visible` (boolean).
    - Style: High-visibility orange/red to match Witcher Senses theme.
    - Animation: Smoothly update the strokeDashoffset based on progress.
  </action>
  <verify>
    <automated>Check for SVG progress ring logic in ScanningProgress.tsx.</automated>
  </verify>
  <done>Reusable progress ring component created for the discovery HUD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component -> UI | Visual only, no trust boundaries crossed |
</threat_model>

<verification>
Review `ClueBillboard.tsx` for `ViroText` implementation and `ScanningProgress.tsx` for SVG logic.
</verification>

<success_criteria>
- ClueBillboard correctly renders hover text within range.
- ScanningProgress component correctly animates from 0% to 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/09-proximity-discovery/09-02-SUMMARY.md`
</output>
