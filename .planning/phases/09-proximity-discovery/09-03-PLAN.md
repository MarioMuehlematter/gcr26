---
phase: 09-proximity-discovery
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified: [apps/mobile/src/screens/ARScreen.tsx, apps/mobile/src/components/HuntingHUD.tsx]
autonomous: true
requirements: [PLAY-04]

must_haves:
  truths:
    - "Discovery progress is visible in the HUD when focusing on a clue"
    - "Clues automatically update to 'discovered' state when within 1.5m and in focus"
    - "System only functions when Witcher Senses are active"
  artifacts:
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "Main integration of proximity discovery logic"
    - path: "apps/mobile/src/components/HuntingHUD.tsx"
      provides: "Visual scanning feedback in HUD"
---

<objective>
Finalize the proximity discovery feature by integrating the logic and visuals into the main player experience.

Purpose: To connect all components into a functional gameplay loop.
Output: Fully wired `ARScreen.tsx` and `HuntingHUD.tsx`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/screens/ARScreen.tsx
@apps/mobile/src/components/HuntingHUD.tsx
@.planning/phases/09-proximity-discovery/09-01-SUMMARY.md
@.planning/phases/09-proximity-discovery/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update HuntingHUD with Scanning Progress</name>
  <files>apps/mobile/src/components/HuntingHUD.tsx</files>
  <action>
    Update HuntingHUD:
    - Accept `discoveryProgress` and `isScanning` props.
    - Render `ScanningProgress` component in the center of the HUD.
    - Style the scanner as an overlay that appears when `isScanning` is true.
  </action>
  <verify>
    <automated>Verify HuntingHUD renders ScanningProgress conditionally.</automated>
  </verify>
  <done>Player receives real-time visual feedback during the discovery focus period.</done>
</task>

<task type="auto">
  <name>Wire Proximity Discovery into ARScreen</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    Integrate discovery into the main AR loop:
    - Initialize `useProximityDiscovery` with camera transform and senses state.
    - Extract `{ isFocused, discoveryProgress, focusTargetId }`.
    - Pass `discoveryProgress` to `HuntingHUD`.
    - Pass `cameraPosition` and discovery status to `ClueBillboard` in the AR scene.
    - Ensure `discoverClue` is called via the hook's automatic logic.
  </action>
  <verify>
    <automated>Check ARScreen for useProximityDiscovery usage and prop wiring.</automated>
  </verify>
  <done>AR Scene and HUD correctly respond to player movement and focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic -> UI | Wired state propagates to display components |
</threat_model>

<verification>
Check `ARScreen.tsx` for complete wiring of the discovery hook and HUD.
</verification>

<success_criteria>
- Discovery progress ring appears in HUD when 1.5m away and in FOV.
- Clue Billboard labels appear at 3m.
- Team discovery state is updated when the 1s focus completes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-proximity-discovery/09-03-SUMMARY.md`
</output>
