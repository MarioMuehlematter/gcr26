---
phase: 09-proximity-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/src/hooks/useProximityDiscovery.ts]
autonomous: true
requirements: [PLAY-04]

must_haves:
  truths:
    - "Hook calculates real-time distance and angle to the target clue"
    - "Hook maintains a 1-second focus timer for auto-discovery"
    - "Discovery trigger fires only when Witcher Senses are active and in range"
  artifacts:
    - path: "apps/mobile/src/hooks/useProximityDiscovery.ts"
      provides: "Auto-discovery state and timer logic"
---

<objective>
Implement the core proximity discovery engine that tracks player focus on the next investigation target and manages the auto-discovery timer.

Purpose: To ensure players must physically approach and focus on clues to progress.
Output: A reusable React hook `useProximityDiscovery`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/hooks/useClueGuidance.ts
@apps/mobile/src/hooks/useInvestigation.ts
</context>

<tasks>

<task type="auto">
  <name>Create useProximityDiscovery hook</name>
  <files>apps/mobile/src/hooks/useProximityDiscovery.ts</files>
  <action>
    Implement a hook that calculates distance and angle to the 'nextClueId' (from useClueGuidance patterns).
    - Thresholds: discoveryRange = 1.5m, fovThreshold = 15 degrees.
    - Logic: 
      - If witcherSensesActive AND distance <= discoveryRange AND Math.abs(angle) <= fovThreshold AND !isDiscovered:
        - Increment progress from 0 to 1 over 1000ms.
      - Else: 
        - Reset progress to 0.
    - Return: { isFocused, discoveryProgress, focusTargetId }.
  </action>
  <verify>
    <automated>Check for export and core logic structures in hook file.</automated>
  </verify>
  <done>Hook logic implemented with 1-second timer and FOV gating.</done>
</task>

<task type="auto">
  <name>Integrate Discovery Trigger</name>
  <files>apps/mobile/src/hooks/useProximityDiscovery.ts</files>
  <action>
    Use useEffect to monitor discoveryProgress. 
    When progress reaches 1.0, call `discoverClue(focusTargetId)` (leveraging useInvestigation).
    Ensure the trigger only fires once per clue discovery.
  </action>
  <verify>
    <automated>Verify discoverClue call is present in a useEffect block.</automated>
  </verify>
  <done>Discovery event fires automatically upon successful 1s focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic -> Firestore | Auto-discovery triggers a write to the team's discoveredClueIds |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Discovery Hook | mitigate | Use client-side debounce and range checks to prevent accidental/glitched discovery |
| T-09-01-02 | Spoofing | Camera Transform | accept | Coordinate spoofing is possible but low impact for MVP narrative progress |
</threat_model>

<verification>
Check `apps/mobile/src/hooks/useProximityDiscovery.ts` for distance calculations and discovery trigger logic.
</verification>

<success_criteria>
- Hook successfully calculates distance and angle relative to camera.
- Discovery progress increments correctly based on proximity and FOV.
- `discoverClue` is called exactly once when progress hits 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/09-proximity-discovery/09-01-SUMMARY.md`
</output>
