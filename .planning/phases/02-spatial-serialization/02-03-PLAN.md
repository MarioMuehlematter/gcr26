---
phase: 02-spatial-serialization
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [apps/mobile/src/hooks/useARSession.ts, apps/mobile/src/components/RelocalizationOverlay.tsx, apps/mobile/src/screens/ARScreen.tsx]
autonomous: false
requirements: [CORE-03]

must_haves:
  truths:
    - "AR Session can enter a RELOCALIZING state when loading a map"
    - "A visual overlay informs the user that alignment is in progress"
    - "A 15-second timeout handles relocalization failures"
    - "The user can trigger a map save and reload sequence"
  artifacts:
    - path: "apps/mobile/src/hooks/useARSession.ts"
      provides: "Relocalization state and save/load triggers"
    - path: "apps/mobile/src/components/RelocalizationOverlay.tsx"
      provides: "Visual feedback during alignment"
  key_links:
    - from: "apps/mobile/src/hooks/useARSession.ts"
      to: "apps/mobile/src/services/mapService.ts"
      via: "Service calls"
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "apps/mobile/src/components/RelocalizationOverlay.tsx"
      via: "Conditional rendering"
---

<objective>
Integrate map serialization and relocalization logic into the AR session lifecycle and provide user feedback during the alignment process.

Purpose: Complete the end-to-end loop of saving physical environments and restoring sessions with proper UX (CORE-03).
Output: Updated AR hook, new overlay component, and integrated AR screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spatial-serialization/02-CONTEXT.md
@.planning/phases/02-spatial-serialization/02-02-SUMMARY.md
@apps/mobile/src/hooks/useARSession.ts
@apps/mobile/src/screens/ARScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useARSession Hook with Relocalization</name>
  <files>apps/mobile/src/hooks/useARSession.ts</files>
  <action>
    Update `useARSession.ts` to include:
    1. New state variables: `relocalizing` (boolean), `relocalizationStatus` ('NONE' | 'ACTIVE' | 'SUCCESS' | 'FAILED').
    2. `saveCurrentMap(name: string)` method:
       - Trigger map serialization (if supported by the AR engine).
       - Call `mapService.saveMap`.
    3. `loadMapAndRelocalize(mapId: string)` method:
       - Call `mapService.getMap`.
       - Set `relocalizing` to true.
       - Pass map data to the AR Scene (via `viroAppProps` or direct state).
       - Start a 15-second timer (D-05).
    4. Update `onTrackingUpdated`:
       - Detect when relocalization is successful (if the engine provides this signal).
       - Clear the timeout on success.
    
    *Implementation Note:* For Viro, loading an ARWorldMap on iOS is usually done via a prop on `ViroARScene` or a native bridge call. If the direct API is unclear, implement the state management skeleton.
  </action>
  <verify>
    <automated>grep "relocalizing" apps/mobile/src/hooks/useARSession.ts</automated>
  </verify>
  <done>Hook supports relocalization states and map I/O triggers.</done>
</task>

<task type="auto">
  <name>Task 2: Create Relocalization Overlay Component</name>
  <files>apps/mobile/src/components/RelocalizationOverlay.tsx</files>
  <action>
    Create a new component `RelocalizationOverlay.tsx`.
    It should display a full-screen semi-transparent overlay with:
    - An animated loader or pulsing icon.
    - Text: "Aligning with environment..."
    - Instructions: "Move your device slowly around the room."
    - A countdown or progress indicator for the 15s timeout.
    
    Follow the `SignUpModal.tsx` pattern for overlays.
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/RelocalizationOverlay.tsx</automated>
  </verify>
  <done>Overlay component created with proper UX feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate into ARScreen and Verify Loop</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <behavior>
    - When `relocalizing` is true, `RelocalizationOverlay` must be visible.
    - Added "Save Map" and "Load Last Map" buttons for testing purposes (Phase 2 MVP).
  </behavior>
  <action>
    Update `ARScreen.tsx`:
    1. Import `RelocalizationOverlay` and `mapService`.
    2. Conditionally render `RelocalizationOverlay` based on `relocalizing` state from the hook.
    3. Add temporary debug buttons in the HUD or a separate menu to trigger `saveCurrentMap` and `loadMapAndRelocalize` using the last saved ID from MMKV.
  </action>
  <verify>
    <automated>grep "RelocalizationOverlay" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>ARScreen integrated with relocalization logic and visual feedback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Spatial Serialization loop (Save -> Restart -> Load -> Relocalize).</what-built>
  <how-to-verify>
    1. Open ARScreen and scan the room until 'TRACKING' is achieved.
    2. Press the "Save Map" button.
    3. Force close the app.
    4. Re-open the app and go to ARScreen.
    5. Press "Load Last Map".
    6. Verify that the "Aligning with environment..." overlay appears.
    7. Verify that tracking is restored (overlay disappears) when scanning the same area.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Hook | User triggers potentially long-running relocalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | useARSession.ts | mitigate | Implement the 15s timeout to prevent UI hang if relocalization fails. |
</threat_model>

<verification>
Ensure the overlay appears and disappears correctly according to the relocalization state. Ensure buttons correctly trigger service calls.
</verification>

<success_criteria>
1. `useARSession` manages relocalization state and timeout.
2. `RelocalizationOverlay` provides clear visual guidance.
3. User can successfully save a map and re-align after a restart (verified manually).
</success_criteria>

<output>
After completion, create `.planning/phases/02-spatial-serialization/02-03-SUMMARY.md`
</output>
