---
phase: 02-spatial-serialization
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [apps/mobile/src/hooks/useARSession.ts, apps/mobile/src/screens/ARScreen.tsx]
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "AR Session lifecycle supports relocalization state transitions"
    - "Map data can be passed from the storage layer to the AR engine"
    - "The 15-second relocalization timeout triggers a recovery state"
  artifacts:
    - path: "apps/mobile/src/hooks/useARSession.ts"
      provides: "Relocalization state and save/load triggers"
  key_links:
    - from: "apps/mobile/src/hooks/useARSession.ts"
      to: "apps/mobile/src/services/mapService.ts"
      via: "Service calls"
---

<objective>
Integrate map serialization and relocalization logic into the AR session lifecycle.

Purpose: Implement the state management and I/O triggers needed to restore sessions from saved maps (CORE-03).
Output: Updated AR hook and integrated debug triggers in the AR screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spatial-serialization/02-CONTEXT.md
@.planning/phases/02-spatial-serialization/02-02-SUMMARY.md
@apps/mobile/src/hooks/useARSession.ts
@apps/mobile/src/screens/ARScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useARSession Hook with Relocalization Logic</name>
  <files>apps/mobile/src/hooks/useARSession.ts</files>
  <action>
    Update `useARSession.ts` to include:
    1. New state variables: `relocalizing` (boolean), `relocalizationStatus` ('NONE' | 'ACTIVE' | 'SUCCESS' | 'FAILED').
    2. `saveCurrentMap(name: string)` method:
       - Trigger map serialization (if supported by the AR engine).
       - Call `mapService.saveMap`.
    3. `loadMapAndRelocalize(mapId: string)` method:
       - Call `mapService.getMap`.
       - Set `relocalizing` to true.
       - Pass map data to the AR Scene.
       - Start a 15-second timer (D-05).
    4. Update `onTrackingUpdated`:
       - Detect when relocalization is successful.
       - Clear the timeout on success.
  </action>
  <verify>
    <automated>grep "relocalizing" apps/mobile/src/hooks/useARSession.ts</automated>
  </verify>
  <done>Hook supports relocalization states and map I/O triggers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Debug Triggers to ARScreen</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <behavior>
    - ARScreen must expose "Save Map" and "Load Last Map" buttons for testing.
  </behavior>
  <action>
    Update `ARScreen.tsx`:
    1. Import `mapService`.
    2. Add temporary debug buttons in the HUD to trigger `saveCurrentMap` and `loadMapAndRelocalize` using the last saved ID from MMKV.
  </action>
  <verify>
    <automated>grep "saveCurrentMap" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>ARScreen integrated with debug buttons for serialization testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Hook | User triggers potentially long-running relocalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | useARSession.ts | mitigate | Implement the 15s timeout to prevent UI hang if relocalization fails. |
</threat_model>

<verification>
Ensure buttons correctly trigger service calls and hook state changes.
</verification>

<success_criteria>
1. `useARSession` manages relocalization state and timeout.
2. User can trigger save and load actions from the ARScreen HUD.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spatial-serialization/02-03-SUMMARY.md`
</output>
