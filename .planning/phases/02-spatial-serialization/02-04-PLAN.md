---
phase: 02-spatial-serialization
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: [apps/mobile/src/components/RelocalizationOverlay.tsx, apps/mobile/src/screens/ARScreen.tsx]
autonomous: false
requirements: [CORE-03]

must_haves:
  truths:
    - "A visual overlay informs the user that alignment is in progress"
    - "The user receives clear guidance on how to facilitate relocalization"
    - "The application provides immediate feedback upon successful environment alignment"
  artifacts:
    - path: "apps/mobile/src/components/RelocalizationOverlay.tsx"
      provides: "Visual feedback during alignment"
  key_links:
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "apps/mobile/src/components/RelocalizationOverlay.tsx"
      via: "Conditional rendering"
---

<objective>
Provide user feedback during the alignment process and verify the end-to-end spatial serialization loop.

Purpose: Complete the UX for restoring sessions with proper visual guidance (CORE-03).
Output: New overlay component and integrated AR screen UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spatial-serialization/02-CONTEXT.md
@.planning/phases/02-spatial-serialization/02-03-SUMMARY.md
@apps/mobile/src/hooks/useARSession.ts
@apps/mobile/src/screens/ARScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Relocalization Overlay Component</name>
  <files>apps/mobile/src/components/RelocalizationOverlay.tsx</files>
  <action>
    Create a new component `RelocalizationOverlay.tsx`.
    It should display a full-screen semi-transparent overlay with:
    - An animated loader or pulsing icon.
    - Text: "Aligning with environment..."
    - Instructions: "Move your device slowly around the room."
    - A progress indicator for the 15s timeout.
    
    Follow the `SignUpModal.tsx` pattern for overlays.
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/RelocalizationOverlay.tsx</automated>
  </verify>
  <done>Overlay component created with proper UX feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Overlay into ARScreen</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <behavior>
    - When `relocalizing` is true, `RelocalizationOverlay` must be visible.
  </behavior>
  <action>
    Update `ARScreen.tsx`:
    1. Import `RelocalizationOverlay`.
    2. Conditionally render `RelocalizationOverlay` based on `relocalizing` state from the hook.
  </action>
  <verify>
    <automated>grep "RelocalizationOverlay" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>ARScreen integrated with visual feedback for relocalization.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify End-to-End Serialization Loop</name>
  <files>N/A</files>
  <action>Manual verification of the end-to-end serialization loop (Save -> Restart -> Load -> Relocalize).</action>
  <verify>Human approval via resume signal.</verify>
  <done>User confirmed that maps can be saved, loaded, and sessions restored.</done>
  <what-built>End-to-end Spatial Serialization loop (Save -> Restart -> Load -> Relocalize).</ what-built>
  <how-to-verify>
    1. Open ARScreen and scan the room until 'TRACKING' is achieved.
    2. Press the "Save Map" button (added in Plan 03).
    3. Force close the app.
    4. Re-open the app and go to ARScreen.
    5. Press "Load Last Map".
    6. Verify that the "Aligning with environment..." overlay appears.
    7. Verify that tracking is restored (overlay disappears) when scanning the same area.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device Screen | Visual information presented to the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | RelocalizationOverlay | accept | Overlay shows public instructions; no sensitive data. |
</threat_model>

<verification>
Ensure the overlay appears and disappears correctly according to the relocalization state.
</verification>

<success_criteria>
1. `RelocalizationOverlay` provides clear visual guidance.
2. User can successfully save a map and re-align after a restart (verified manually).
</success_criteria>

<output>
After completion, create `.planning/phases/02-spatial-serialization/02-04-SUMMARY.md`
</output>
