---
phase: 04-clue-placement-tool
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [apps/mobile/src/screens/PlacementScreen.tsx, apps/mobile/src/navigation/index.tsx, apps/mobile/src/screens/HomeScreen.tsx, apps/mobile/src/components/MapSelectionModal.tsx]
autonomous: false
requirements: [ADM-02, CORE-01]

must_haves:
  truths:
    - "GMs can select a specific map/site to author clues for"
    - "ClueTray is visible in the HUD for asset selection (Checker Issue 2)"
    - "Placement is only allowed once the landmark origin is locked (Checker Issue 4)"
    - "Tapping a detected plane drops the selected clue at a relative offset (D-03)"
    - "Clues snap to planes with a green highlight feedback (Checker Issue 3)"
    - "Two-finger twist gesture rotates the placed clues (D-02)"
  artifacts:
    - path: "apps/mobile/src/screens/PlacementScreen.tsx"
      provides: "AR placement environment with stabilized origin and ghost preview"
    - path: "apps/mobile/src/components/MapSelectionModal.tsx"
      provides: "UI to choose site/map for authoring"
  key_links:
    - from: "apps/mobile/src/screens/PlacementScreen.tsx"
      to: "apps/mobile/src/hooks/useRecorderSession.ts"
      via: "landmark stabilization gating"
    - from: "apps/mobile/src/screens/PlacementScreen.tsx"
      to: "apps/mobile/src/components/ClueTray.tsx"
      via: "asset selection"
---

<objective>
Assemble the full Clue Placement interface. This plan integrates the tray, billboards, stabilized recorder session, and relative coordinate placement logic into a new AR screen, enabling GMs to author narrative content with precise physical anchoring and visual feedback.

Purpose: Deliver the functional authoring tool with landmark stabilization and snapping.
Output: Placement screen, map selection UI, and interaction logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clue-placement-tool/04-CONTEXT.md
@.planning/phases/04-clue-placement-tool/04-02-SUMMARY.md
@apps/mobile/src/screens/RecorderScreen.tsx
@apps/mobile/src/navigation/index.tsx
@apps/mobile/src/services/mapService.ts
@apps/mobile/src/hooks/useRecorderSession.ts
@apps/mobile/src/components/LockingProgressRing.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map Selection and Navigation Wiring</name>
  <files>apps/mobile/src/components/MapSelectionModal.tsx, apps/mobile/src/navigation/index.tsx, apps/mobile/src/screens/HomeScreen.tsx</files>
  <action>
    1. Create `apps/mobile/src/components/MapSelectionModal.tsx`:
       - Fetch all spatial maps using `getSpatialMaps()` from `mapService`.
       - Present a list of maps; default to `LAST_MAP_ID`.
       - Callback with the selected `mapId`.
    2. Register `PlacementScreen` in `apps/mobile/src/navigation/index.tsx` (guarded by `isAdmin`).
    3. Add "Place Clues" button to `apps/mobile/src/screens/HomeScreen.tsx` for admins.
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>GMs can navigate to the placement tool and select their site.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Placement Screen HUD and Session Management</name>
  <files>apps/mobile/src/screens/PlacementScreen.tsx</files>
  <read_first>
    - apps/mobile/src/screens/RecorderScreen.tsx
  </read_first>
  <action>
    Create the skeleton for `apps/mobile/src/screens/PlacementScreen.tsx`.
    
    Implementation:
    - Integrate `useRecorderSession` and `useCluePlacement`.
    - HUD Requirements:
      - Render `ClueTray` at the bottom (Addresses Checker Issue 2).
      - "Save" button (calls `syncClueToCloud` for all clues).
      - "Change Map" button (triggers `MapSelectionModal`).
      - Display `LockingProgressRing` when `locking` is true.
    - Logic:
      - Call `loadClues(mapId)` from the hook when the screen mounts or `mapId` changes (Addresses Checker Issue 1).
      - Pass `onImageMarkerFound` from `useRecorderSession` to the `ViroARImageMarker` in the scene.
      - Ensure interaction is gated: UI elements and placement logic should check `locked` state from `useRecorderSession` (Addresses Checker Issue 4).
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>PlacementScreen HUD is fully wired with tray, locking logic, and cloud loading.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AR Interaction and Snap-to-Plane</name>
  <files>apps/mobile/src/screens/PlacementScreen.tsx</files>
  <action>
    Complete the `PlacementScene` in `apps/mobile/src/screens/PlacementScreen.tsx`.
    
    Interaction Logic:
    - **Relative Math (D-03):** When a plane is tapped (`onClick`), calculate `clue_rel_pos = world_pos - landmark_pos`. Store in `useCluePlacement`.
    - **Snap-to-Plane Preview (Checker Issue 3):** 
      - If a `selectedClueType` exists but the user hasn't tapped yet, perform a real-time hit-test against detected planes.
      - Render a "ghost" `ClueBillboard` at the hit-test point.
      - Set `highlighted={true}` on the ghost billboard to show the green highlight snap effect.
    - **Two-Finger Twist (D-02):** Implement `onRotate` on the `ClueBillboard` to update the clue's rotation in the hook's state.
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>AR interaction supports relative anchoring, twist rotation, and snap highlights.</done>
  <acceptance_criteria>
    - Clues appear with green highlight when snapping to planes during placement.
    - Clues are correctly offset from the image landmark.
    - Two-finger rotation works as expected.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint: Verify Clue Placement Tool</name>
  <action>Human verification of the AR placement tool functionality.</action>
  <what-built>The full Clue Placement authoring tool with stabilized origin, tray integration, and snap highlights.</what-built>
  <how-to-verify>
    1. Open the mobile app as an Admin and tap "Place Clues".
    2. Choose a site. Verify existing clues (if any) are loaded.
    3. Scan the physical landmark. Verify `LockingProgressRing` appears and completes.
    4. Select a clue from the `ClueTray`.
    5. Move the camera over a floor plane. Verify a ghost clue with green highlight appears "snapped" to the plane.
    6. Tap to place. Verify it stays at the relative offset.
    7. Twist with two fingers to rotate.
    8. Tap "Save".
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <verify>Manual verification successful</verify>
  <done>All placement features verified in AR</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Authoring Tool | Authenticated admin access to placement UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | Navigation | mitigate | Ensure Placement screen is strictly guarded by `profile.isAdmin` in the navigator |
| T-04-04 | Spoofing | Marker Origin | mitigate | useRecorderSession ensures the origin is stabilized before allowing placement |
</threat_model>

<verification>
Manual verification in AR is essential for CORE-01 (Precise Anchoring).
</verification>

<success_criteria>
1. Existing clues are loaded from cloud on site selection.
2. Clues can be placed, oriented using twist gesture, and saved relative to a physical landmark.
3. Snap-to-plane visual feedback (green highlight) is present during placement.
4. Authoring is gated by landmark stabilization logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-clue-placement-tool/04-03-SUMMARY.md`
</output>
