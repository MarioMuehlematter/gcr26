---
phase: 04-clue-placement-tool
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [apps/mobile/src/hooks/useCluePlacement.ts, apps/mobile/src/components/ClueTray.tsx, apps/mobile/src/components/ClueBillboard.tsx]
autonomous: true
requirements: [ADM-02]

must_haves:
  truths:
    - "GMs can select different clue types defined in clues.json"
    - "Clues are rendered as billboards in the AR scene"
    - "Existing clues are fetched from Firestore when a site is opened"
    - "Placement state is managed via a dedicated hook"
  artifacts:
    - path: "apps/mobile/src/hooks/useCluePlacement.ts"
      provides: "handlePlaceClue, loadClues, and activeClueState"
    - path: "apps/mobile/src/components/ClueTray.tsx"
      provides: "Scrollable list of clue assets from JSON"
    - path: "apps/mobile/src/components/ClueBillboard.tsx"
      provides: "ViroNode wrapper with green highlight support"
---

<objective>
Develop the UI components and business logic for placing clues. This plan creates the interactive "Tray" for asset selection, the AR "Billboard" with highlight support, and the hook for state management and Firestore synchronization.

Purpose: Provide the building blocks for the placement interface with cloud-loading support.
Output: Hook for state management, Tray UI, and AR rendering component with visual feedback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clue-placement-tool/04-CONTEXT.md
@.planning/phases/04-clue-placement-tool/04-01-SUMMARY.md
@packages/shared/src/clues.json
@apps/mobile/src/hooks/useRecorderSession.ts
@apps/mobile/src/services/mapService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Clue Placement Hook with Cloud Loading</name>
  <files>apps/mobile/src/hooks/useCluePlacement.ts</files>
  <read_first>
    - apps/mobile/src/hooks/useRecorderSession.ts
    - apps/mobile/src/services/mapService.ts
  </read_first>
  <action>
    Create `apps/mobile/src/hooks/useCluePlacement.ts`.
    
    Responsibilities:
    - Maintain `placedClues: Clue[]` state.
    - Maintain `selectedClueType: ClueType | null` state.
    - `loadClues(mapId: string)`: Asynchronously fetches existing clues from Firestore using `mapService.getClues` and populates `placedClues` (Addresses Checker Issue 1).
    - `addClue(position: [number, number, number])`: Creates a new Clue object and adds to list.
    - `removeClue(id: string)`: Removes clue from list.
    - `updateClueRotation(id: string, rotation: [number, number, number])`: Updates rotation (D-02).
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>useCluePlacement hook handles both local state and cloud fetching.</done>
  <acceptance_criteria>
    - Hook can fetch existing clues for a given mapId.
    - addClue creates a unique ID and uses the currently selected clue type.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Clue Tray Component from JSON</name>
  <files>apps/mobile/src/components/ClueTray.tsx</files>
  <action>
    Create `apps/mobile/src/components/ClueTray.tsx`.
    
    Implementation details:
    - Import clue types from `packages/shared/src/clues.json`.
    - Render a horizontal scrollable list at the bottom of the screen.
    - Show an icon (using assets from `apps/mobile/assets/clues/`) and name for each clue type.
    - When a type is selected, update the `selectedClueType` in the `useCluePlacement` hook.
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>ClueTray component is ready for use in the placement screen, driven by JSON.</done>
  <acceptance_criteria>
    - Tray dynamically renders types from the JSON config.
    - Selection visual state is clear (e.g. border highlight).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Clue Billboard Component with Highlight Support</name>
  <files>apps/mobile/src/components/ClueBillboard.tsx</files>
  <action>
    Create `apps/mobile/src/components/ClueBillboard.tsx`.
    
    A Viro component that:
    - Wraps `ViroImage` or `ViroQuad`.
    - Takes `clue: Clue` as a prop.
    - Adds an optional `highlighted: boolean` prop (Addresses Checker Issue 3).
    - Applies `position`, `rotation`, and `scale`.
    - Uses `ViroMaterials` to show the correct texture based on `clue.type`.
    - If `highlighted`, applies a `green_highlight` material (subtle green tint/glow).
    - Faces "up" (ground-aligned) by default for decals.
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>ClueBillboard renders clues in the AR scene with highlight support.</done>
  <acceptance_criteria>
    - Component uses the Clue interface for props.
    - Materials are correctly assigned per type.
    - `highlighted` prop successfully toggles the green highlight material.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Hook | User interactions trigger state updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | Memory | mitigate | Limit maximum number of clues per session (e.g. 50) to prevent performance lag |
</threat_model>

<verification>
Ensure all components are correctly typed and follow existing patterns.
</verification>

<success_criteria>
1. All three files are created and the mobile app compiles.
2. Hook logic correctly manages a collection of clues and supports cloud loading.
3. UI components are driven by the shared JSON configuration and support visual feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-clue-placement-tool/04-02-SUMMARY.md`
</output>
