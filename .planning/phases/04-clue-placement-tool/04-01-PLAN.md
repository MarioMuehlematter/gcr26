---
phase: 04-clue-placement-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/shared/src/clues.json, packages/shared/src/types.ts, apps/mobile/src/services/mapService.ts, apps/mobile/assets/clues/footprint.png, apps/mobile/assets/clues/bloodstain.png, apps/mobile/assets/clues/scuff.png]
autonomous: true
requirements: [ADM-02, CORE-01]

must_haves:
  truths:
    - "Clue types are defined in a shared JSON configuration (D-05)"
    - "Placeholder assets exist for all defined clue types"
    - "Map service can persist clues to Firestore subcollections"
  artifacts:
    - path: "packages/shared/src/clues.json"
      provides: "Source of truth for clue types"
    - path: "packages/shared/src/types.ts"
      provides: "Clue and ClueType interfaces derived from JSON"
    - path: "apps/mobile/assets/clues/"
      provides: "Visual assets for clues"
  key_links:
    - from: "apps/mobile/src/services/mapService.ts"
      to: "firestore"
      via: "setDoc"
---

<objective>
Establish the data models, asset library, and persistence layer for AR clues. This foundation ensures that clue types are configurable and clues placed in AR can be serialized and saved to the cloud relative to a physical site map.

Purpose: Enable persistent, cloud-synced clue placement with configurable types.
Output: Clue configuration JSON, updated types, placeholder assets, and service methods.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clue-placement-tool/04-CONTEXT.md
@packages/shared/src/types.ts
@apps/mobile/src/services/mapService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Clue Configuration and Types</name>
  <files>packages/shared/src/clues.json, packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts
  </read_first>
  <action>
    1. Create `packages/shared/src/clues.json` with the following structure (per D-05):
       ```json
       {
         "types": [
           { "id": "footprint", "name": "Footprint", "asset": "footprint.png" },
           { "id": "bloodstain", "name": "Bloodstain", "asset": "bloodstain.png" },
           { "id": "scuff", "name": "Scuff Mark", "asset": "scuff.png" }
         ]
       }
       ```
    2. Update `packages/shared/src/types.ts`:
       - Import `clues.json`.
       - Define `ClueType` as a union of IDs from the JSON (e.g., `typeof clues.types[number]['id']`).
       - Define `Clue`:
         - id: string
         - type: ClueType
         - position: [number, number, number] (x, y, z offsets from landmark)
         - rotation: [number, number, number] (rx, ry, rz relative to origin)
         - scale: [number, number, number]
         - metadata: Record<string, any>
       - Update `SpatialMapMetadata` to include `clueCount: number`.
  </action>
  <verify>
    <automated>npx tsc -p packages/shared/tsconfig.json</automated>
  </verify>
  <done>Clue types are derived from JSON and the shared package compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Provide Placeholder Clue Assets</name>
  <files>apps/mobile/assets/clues/footprint.png, apps/mobile/assets/clues/bloodstain.png, apps/mobile/assets/clues/scuff.png</files>
  <action>
    Create the `apps/mobile/assets/clues/` directory if it doesn't exist.
    Provide placeholder 64x64 PNG images for:
    - `footprint.png`
    - `bloodstain.png`
    - `scuff.png`
    
    (Note: Since this is an automated task, use a script to generate simple colored square PNGs if real assets aren't provided, or just touch the files if they will be replaced later. For this task, create valid minimal PNGs using a tool or provide them.)
  </action>
  <verify>
    <automated>ls apps/mobile/assets/clues/*.png</automated>
  </verify>
  <done>Visual assets for clues exist in the mobile app assets directory.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Map Service for Clue Persistence</name>
  <files>apps/mobile/src/services/mapService.ts</files>
  <read_first>
    - apps/mobile/src/services/mapService.ts
  </read_first>
  <action>
    Add the following functions to `apps/mobile/src/services/mapService.ts`:
    1. `syncClueToCloud(mapId: string, clue: Clue)`:
       - Path: `spatial_maps/{mapId}/clues/{clue.id}`
       - Use `setDoc` with `{ merge: true }`.
    2. `getClues(mapId: string)`:
       - Retrieves all clues from the subcollection.
    3. `getSpatialMaps()`:
       - Retrieves a list of all available spatial maps (to support site selection).
  </action>
  <verify>
    <automated>npx tsc -p apps/mobile/tsconfig.json</automated>
  </verify>
  <done>mapService.ts exports persistence and retrieval methods for clues and maps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Firestore | Client-side writes to clue subcollections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Clue Data | mitigate | Use Firestore Security Rules to validate clue coordinates and types (implemented in next phase) |
</threat_model>

<verification>
Verify types are correctly exported and services compile without errors.
</verification>

<success_criteria>
1. Shared types package compiles with Clue definitions derived from JSON.
2. Placeholder assets are present in the assets directory.
3. Mobile app compiles with updated mapService.
</success_criteria>

<output>
After completion, create `.planning/phases/04-clue-placement-tool/04-01-SUMMARY.md`
</output>
