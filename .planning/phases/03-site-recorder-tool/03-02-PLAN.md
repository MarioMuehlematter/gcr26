---
phase: 03-site-recorder-tool
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [apps/mobile/src/hooks/useRecorderSession.ts, apps/mobile/src/components/LockingProgressRing.tsx, apps/mobile/src/screens/RecorderScreen.tsx]
autonomous: true
requirements: [ADM-01]

must_haves:
  truths:
    - "Pointing the camera at a physical landmark starts a locking process"
    - "A visual progress ring fills over 5 seconds during locking"
    - "The system detects when the lock is successful"
  artifacts:
    - path: "apps/mobile/src/hooks/useRecorderSession.ts"
      provides: "Landmark detection and 5s lock logic"
    - path: "apps/mobile/src/components/LockingProgressRing.tsx"
      provides: "Animated visual feedback for locking"
  key_links:
    - from: "apps/mobile/src/hooks/useRecorderSession.ts"
      to: "apps/mobile/src/components/LockingProgressRing.tsx"
      via: "locking state boolean"
---

<objective>
Implement the core mapping logic: detecting a landmark and requiring the GM to hold focus for 5 seconds to establish the site origin.

Purpose: Ensure a stable and intentional world origin is set for each recorded site (D-01).
Output: A custom hook for session management and an animated progress component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-site-recorder-tool/03-01-SUMMARY.md
@.planning/phases/03-site-recorder-tool/03-CONTEXT.md
@.planning/phases/03-site-recorder-tool/03-PATTERNS.md
@apps/mobile/src/hooks/useARSession.ts
</context>

<tasks>

<task type="auto">
  <name>Create useRecorderSession Hook</name>
  <files>apps/mobile/src/hooks/useRecorderSession.ts</files>
  <action>
    Create a new hook `useRecorderSession.ts` based on the 'Detection Loop Pattern' in 03-PATTERNS.md.
    It must:
    - Manage `locking` (boolean) and `locked` (boolean) states.
    - Expose `onImageMarkerFound` callback.
    - When an image marker is found, if not already locked, set `locking` to true.
    - Provide a `reset` function to restart the scan if needed.
    - Handle the 5-second locking countdown internally or via state triggers.
    - Use `targetImageId` as the anchor identifier.
  </action>
  <verify>
    <automated>ls apps/mobile/src/hooks/useRecorderSession.ts</automated>
  </verify>
  <done>Recorder session hook implemented with locking logic</done>
</task>

<task type="auto">
  <name>Create LockingProgressRing Component</name>
  <files>apps/mobile/src/components/LockingProgressRing.tsx</files>
  <action>
    Create `apps/mobile/src/components/LockingProgressRing.tsx` using the 'Animation Pattern' from 03-PATTERNS.md.
    - Use `Animated` API to fill a circular progress bar.
    - Duration: 5000ms (5 seconds) as per D-01.
    - Accept a `visible` prop and an `onComplete` callback.
    - Display "Locking Origin..." text below the ring.
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/LockingProgressRing.tsx</automated>
  </verify>
  <done>Animated progress ring component created</done>
</task>

<task type="auto">
  <name>Integrate Locking Logic into RecorderScreen</name>
  <files>apps/mobile/src/screens/RecorderScreen.tsx</files>
  <action>
    1. Update `RecorderScreen.tsx` to use the `useRecorderSession` hook.
    2. Pass `onImageMarkerFound` to the `ViroARScene` via `viroAppProps`.
    3. Within the AR scene, render a `ViroARImageMarker` that triggers the callback.
    4. Render the `LockingProgressRing` overlay when `locking` is true.
    5. Handle the `onComplete` callback from the progress ring to set the `locked` state in the hook/screen.
  </action>
  <verify>
    <automated>grep "LockingProgressRing" apps/mobile/src/screens/RecorderScreen.tsx</automated>
  </verify>
  <done>Recorder screen integrated with locking logic and visual feedback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AR Engine → Hook | Native AR anchor detection events trigger state changes in the React hook. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | Camera Feed | accept | Standard AR permissions apply; no PII is captured or sent to the cloud in this phase beyond landmark ID. |
</threat_model>

<verification>
1. Open the Recorder screen as an admin.
2. Point at the designated physical marker.
3. Verify the progress ring appears and fills over 5 seconds.
4. Verify that moving away from the marker stops/resets the process (optional but recommended in implementation).
</verification>

<success_criteria>
- Landmark detection triggers the 5-second countdown.
- Progress ring provides clear visual feedback to the GM.
- Session enters 'locked' state upon completion of the countdown.
</success_criteria>

<output>
After completion, create `.planning/phases/03-site-recorder-tool/03-02-SUMMARY.md`
</output>
