---
phase: 03-site-recorder-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/shared/src/types.ts, apps/mobile/src/navigation/index.tsx, apps/mobile/src/screens/HomeScreen.tsx, apps/mobile/src/screens/RecorderScreen.tsx]
autonomous: true
requirements: [ADM-01]

must_haves:
  truths:
    - "Admins see a 'Record New Site' button on the Home screen"
    - "Only Admins can navigate to the Recorder screen"
    - "The Recorder screen opens with a functional AR camera view"
  artifacts:
    - path: "apps/mobile/src/screens/RecorderScreen.tsx"
      provides: "Skeleton AR view for site recording"
    - path: "apps/mobile/src/navigation/index.tsx"
      provides: "Admin-guarded route for Recorder"
  key_links:
    - from: "apps/mobile/src/screens/HomeScreen.tsx"
      to: "apps/mobile/src/screens/RecorderScreen.tsx"
      via: "navigation.navigate('Recorder')"
---

<objective>
Setup the foundation for the Site Recorder tool, including the data model updates, admin-only access guards, and the initial screen skeleton.

Purpose: Ensure only authorized Game Masters can access the mapping tools and provide a starting point for the AR scanning logic.
Output: Updated shared types, admin-guarded navigation, and a skeleton Recorder screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-site-recorder-tool/03-CONTEXT.md
@.planning/phases/03-site-recorder-tool/03-PATTERNS.md
@apps/mobile/src/navigation/index.tsx
@apps/mobile/src/screens/HomeScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Update SpatialMapMetadata Type</name>
  <files>packages/shared/src/types.ts</files>
  <action>
    Add `targetImageId: string` to the `SpatialMapMetadata` interface in `packages/shared/src/types.ts`.
    This field is required for image landmark relocalization as per D-04.
  </action>
  <verify>
    <automated>grep "targetImageId: string" packages/shared/src/types.ts</automated>
  </verify>
  <done>Interface updated with targetImageId field</done>
</task>

<task type="auto">
  <name>Add Recorder Route with Admin Guard</name>
  <files>apps/mobile/src/navigation/index.tsx</files>
  <action>
    1. Import `useUser` from `../hooks/useUser`.
    2. Update `RootNavigator` to fetch `profile` from `useUser()`.
    3. Update the conditional rendering in `Stack.Navigator` to include `<Stack.Screen name="Recorder" component={RecorderScreen} />` only if `profile?.isAdmin` is true (D-05).
    4. Ensure `loading` from `useUser` is handled in the initial check.
    5. Follow the 'Admin Guard Pattern' from 03-PATTERNS.md.
  </action>
  <verify>
    <automated>grep "name=\"Recorder\"" apps/mobile/src/navigation/index.tsx</automated>
  </verify>
  <done>Navigation updated with guarded Recorder route</done>
</task>

<task type="auto">
  <name>Add Site Recorder Button to Home Screen</name>
  <files>apps/mobile/src/screens/HomeScreen.tsx</files>
  <action>
    In `apps/mobile/src/screens/HomeScreen.tsx`, within the `if (profile?.isAdmin)` block, add a new `TouchableOpacity` button labeled "Record New Site".
    This button should navigate to the 'Recorder' screen.
    Style it consistently with other admin buttons.
  </action>
  <verify>
    <automated>grep "navigate('Recorder')" apps/mobile/src/screens/HomeScreen.tsx</automated>
  </verify>
  <done>Home screen displays Recorder button for admins</done>
</task>

<task type="auto">
  <name>Create Skeleton Recorder Screen</name>
  <files>apps/mobile/src/screens/RecorderScreen.tsx</files>
  <action>
    Create `apps/mobile/src/screens/RecorderScreen.tsx` using the pattern from `ARScreen.tsx`.
    It should include:
    - `ViroARSceneNavigator` with an initial scene.
    - A back button to return to the Home screen.
    - A basic HUD or message saying "Scanning mode active".
    Keep the scene simple for now (no custom logic yet).
  </action>
  <verify>
    <automated>ls apps/mobile/src/screens/RecorderScreen.tsx</automated>
  </verify>
  <done>Skeleton Recorder screen created and functional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Navigation | Route protection happens on the client based on Firestore profile data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Navigation Guard | mitigate | Implement strict check on `profile.isAdmin` before rendering the Recorder screen route. |
</threat_model>

<verification>
1. Login as an admin user.
2. Verify 'Record New Site' button is visible on Home.
3. Tap button and verify it navigates to the (skeleton) Recorder screen.
4. Login as a non-admin user and verify the button is NOT visible.
</verification>

<success_criteria>
- Navigation guard prevents non-admins from accessing Recorder route.
- Recorder screen displays AR view with a back button.
- Shared types updated for downstream persistence tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-site-recorder-tool/03-01-SUMMARY.md`
</output>
