---
phase: 03-site-recorder-tool
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [apps/mobile/src/services/mapService.ts, apps/mobile/src/components/SiteNamingModal.tsx, apps/mobile/src/screens/RecorderScreen.tsx]
autonomous: true
requirements: [ADM-01]

must_haves:
  truths:
    - "GMs can provide a human-readable name for the recorded site"
    - "The site metadata and landmark ID are saved to Firestore"
    - "GMs receive confirmation when the site is successfully saved"
  artifacts:
    - path: "apps/mobile/src/components/SiteNamingModal.tsx"
      provides: "Form for site naming and cloud sync trigger"
    - path: "apps/mobile/src/services/mapService.ts"
      provides: "Function to sync local metadata to Firestore"
  key_links:
    - from: "apps/mobile/src/services/mapService.ts"
      to: "Firestore (spatial_maps)"
      via: "setDoc"
---

<objective>
Complete the Site Recorder by enabling GMs to name and persist the recorded world origin to the cloud.

Purpose: Store the physical site configuration in a centralized database (Firestore) so it can be used for clue placement and player relocalization (D-03, D-06).
Output: Updated map service for cloud sync, a naming modal, and the completed save flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-site-recorder-tool/03-02-SUMMARY.md
@.planning/phases/03-site-recorder-tool/03-CONTEXT.md
@.planning/phases/03-site-recorder-tool/03-PATTERNS.md
@apps/mobile/src/services/mapService.ts
</context>

<tasks>

<task type="auto">
  <name>Implement syncMapToCloud in MapService</name>
  <files>apps/mobile/src/services/mapService.ts</files>
  <action>
    Add a new function `syncMapToCloud` to `apps/mobile/src/services/mapService.ts` based on the 'Firestore Sync Pattern' in 03-PATTERNS.md.
    - Accept `SpatialMapMetadata` and `targetImageId` (string).
    - Save the document to a root `spatial_maps` collection in Firestore.
    - Use `serverTimestamp()` for `cloudSyncAt`.
    - Ensure the document includes all required metadata fields (D-03, D-04).
  </action>
  <verify>
    <automated>grep "syncMapToCloud" apps/mobile/src/services/mapService.ts</automated>
  </verify>
  <done>Cloud sync functionality added to Map Service</done>
</task>

<task type="auto">
  <name>Create SiteNamingModal Component</name>
  <files>apps/mobile/src/components/SiteNamingModal.tsx</files>
  <action>
    Create `apps/mobile/src/components/SiteNamingModal.tsx` using the 'Form & Validation Pattern' from 03-PATTERNS.md.
    - Provide a `TextInput` for the site name (D-06).
    - Provide a "Save to Cloud" button.
    - Handle loading state and display errors if the Firestore write fails.
    - Accept an `onSave` callback that receives the name.
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/SiteNamingModal.tsx</automated>
  </verify>
  <done>Site naming and save modal created</done>
</task>

<task type="auto">
  <name>Implement Final Save Flow in RecorderScreen</name>
  <files>apps/mobile/src/screens/RecorderScreen.tsx</files>
  <action>
    Complete `RecorderScreen.tsx` by integrating the save flow:
    1. When the `locked` state is reached (from 03-02), show the `SiteNamingModal`.
    2. When the GM provides a name and taps "Save":
       - Call `mapService.saveMap` locally (to get initial metadata).
       - Call `mapService.syncMapToCloud` with the metadata and `targetImageId`.
       - Show an `Alert` on success and navigate back to the Home screen.
    3. Add a "Cancel" option in the naming modal to reset the scan.
  </action>
  <verify>
    <automated>grep "SiteNamingModal" apps/mobile/src/screens/RecorderScreen.tsx</automated>
  </verify>
  <done>Complete Site Recorder flow implemented and verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Authenticated admin user writes spatial metadata to the cloud. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Spatial Maps | mitigate | Rely on Firestore security rules (to be implemented separately or ensured by existing admin rules) to prevent non-admins from writing to `spatial_maps`. |
</threat_model>

<verification>
1. Complete a 5-second lock on a landmark.
2. Provide a name like "Grand Hall" in the naming modal.
3. Tap "Save to Cloud".
4. Verify the success alert appears and you are returned to the Home screen.
5. (Optional) Check the Firestore Emulator or Dashboard to confirm the document exists in `spatial_maps`.
</verification>

<success_criteria>
- Site name is captured from the GM.
- Spatial metadata (including targetImageId) is stored in Firestore.
- User is returned to Home screen after a successful save.
</success_criteria>

<output>
After completion, create `.planning/phases/03-site-recorder-tool/03-03-SUMMARY.md`
</output>
