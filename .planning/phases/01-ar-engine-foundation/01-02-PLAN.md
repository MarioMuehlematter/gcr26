---
phase: "01-ar-engine-foundation"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01"]
files_modified: ["apps/mobile/src/screens/ARScreen.tsx", "apps/mobile/src/hooks/useARSession.ts"]
autonomous: true
requirements: ["CORE-02"]

must_haves:
  truths:
    - "AR camera feed is visible on screen."
    - "Tracking status is accurately reported (Tracking/Limited/Initializing)."
    - "A 3D marker appears at the world origin (0,0,0) and remains stable."
  artifacts:
    - path: "apps/mobile/src/hooks/useARSession.ts"
      provides: "AR tracking state management"
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "Live AR viewport"
  key_links:
    - from: "ARScreen"
      to: "ViroARSceneNavigator"
      via: "Component rendering"
    - from: "ARScreen"
      to: "useARSession"
      via: "Hook consumption"
---

<objective>
Implement the core AR scene and tracking monitor. 
This establishes the SLAM tracking session and places a stable world origin marker to verify drift levels as per D-01 and D-02.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-ar-engine-foundation/01-CONTEXT.md
@.planning/phases/01-ar-engine-foundation/01-PATTERNS.md
@apps/mobile/src/screens/ARScreen.tsx
@.planning/research/PITFALLS.md

<interfaces>
From @reactvision/react-viro:
```typescript
import {
  ViroARSceneNavigator,
  ViroARScene,
  ViroText,
  ViroBox,
  ViroNode,
  ViroMaterials,
  ViroTrackingStateConstants,
} from '@reactvision/react-viro';

// Scene Navigator usage
<ViroARSceneNavigator
  initialScene={{ scene: InitialARScene }}
  style={{ flex: 1 }}
/>

// Tracking Updated Callback
const onTrackingUpdated = (state: any, reason: any) => {
  // ViroTrackingStateConstants.TRACKING_NORMAL
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Implement AR Tracking Hook</name>
  <read_first>apps/mobile/src/hooks/useAuth.ts</read_first>
  <files>apps/mobile/src/hooks/useARSession.ts</files>
  <action>
    1. Create `useARSession.ts` following the hook pattern in PATTERNS.md.
    2. Implement state for `trackingStatus` (string) and `isInitialized` (boolean).
    3. Export a function `onTrackingUpdated(state, reason)` that updates the tracking status.
    4. Map `ViroTrackingStateConstants` to human-readable strings: 'TRACKING', 'LIMITED', 'INITIALIZING'.
  </action>
  <verify>
    <automated>grep "trackingStatus" apps/mobile/src/hooks/useARSession.ts && grep "onTrackingUpdated" apps/mobile/src/hooks/useARSession.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hook exports trackingStatus and isInitialized.
    - Hook exports an onTrackingUpdated callback.
  </acceptance_criteria>
  <done>AR session logic is encapsulated in a reusable hook.</done>
</task>

<task type="auto">
  <name>Implement AR Scene with World Origin Marker</name>
  <read_first>apps/mobile/src/screens/ARScreen.tsx</read_first>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    1. Update `ARScreen.tsx` to include an internal `MainScene` component (ViroARScene).
    2. Wire `ViroARSceneNavigator` to render `MainScene`.
    3. In `MainScene`, use `onTrackingUpdated` from `useARSession`.
    4. Place a `ViroBox` (scale [0.1, 0.1, 0.1]) at position [0, 0, 0] to serve as the stable world origin (D-02).
    5. Add a `ViroText` above the box labeled "ORIGIN".
    6. Overlay a HUD on top of the AR view showing the current tracking status from `useARSession`.
  </action>
  <verify>
    <automated>grep "ViroARSceneNavigator" apps/mobile/src/screens/ARScreen.tsx && grep "position=\[0, 0, 0\]" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ARScreen renders the ViroARSceneNavigator.
    - A 3D marker is defined at the origin [0,0,0].
    - Tracking status HUD is visible on screen.
  </acceptance_criteria>
  <done>The AR scene is live and the world origin is marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> AR View | User physical movement translated into digital camera movement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | MainScene | mitigate | Monitor frame rate; provide a 'Limited Tracking' warning if SLAM performance drops. |
| T-01-04 | Spoofing | Tracking Status | mitigate | Derive status directly from ViroTrackingStateConstants rather than intermediate JS logic. |
</threat_model>

<verification>
- Open the AR screen.
- Verify camera feed is active.
- Verify the "ORIGIN" box appears where the phone was when the scene started.
- Move around and ensure the box stays fixed in the physical world (minimal drift).
</verification>

<success_criteria>
- AR session is active and stable.
- Tracking state is correctly displayed to the user.
- World origin is established and persistent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ar-engine-foundation/01-02-SUMMARY.md`
</output>
