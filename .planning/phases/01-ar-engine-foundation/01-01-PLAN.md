---
phase: "01-ar-engine-foundation"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["apps/mobile/package.json", "apps/mobile/app.json", "apps/mobile/src/navigation/index.tsx", "apps/mobile/src/screens/ARScreen.tsx"]
autonomous: false
requirements: ["CORE-02"]
user_setup:
  - service: "Expo"
    why: "Native AR modules require a custom development build."
    dashboard_config:
      - task: "Rebuild development client"
        location: "Run 'npx expo run:ios' or 'npx expo run:android' after config changes"

must_haves:
  truths:
    - "ViroReact dependencies are installed and configured."
    - "AR Screen is accessible via the main navigation."
    - "App requests camera permissions upon entering AR mode."
  artifacts:
    - path: "apps/mobile/app.json"
      provides: "ViroReact plugin configuration"
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "AR view skeleton"
  key_links:
    - from: "RootNavigator"
      to: "ARScreen"
      via: "Stack.Screen registration"
---

<objective>
Setup the foundational AR environment and navigation. 
This involves installing the ViroReact library, configuring the Expo plugin for native module support, and creating the entry point for the AR experience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-ar-engine-foundation/01-CONTEXT.md
@.planning/phases/01-ar-engine-foundation/01-PATTERNS.md
@apps/mobile/package.json
@apps/mobile/app.json
@apps/mobile/src/navigation/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Install AR Dependencies & Configure Plugin</name>
  <read_first>apps/mobile/package.json, apps/mobile/app.json</read_first>
  <files>apps/mobile/package.json, apps/mobile/app.json</files>
  <action>
    1. Install `@reactvision/react-viro` version compatible with Expo SDK 54 (approx ^2.41.0).
    2. Update `apps/mobile/app.json` to include the `@reactvision/react-viro` plugin in the `plugins` array.
    3. Add `NSCameraUsageDescription` to `ios.infoPlist` in `app.json`.
    4. Ensure `liDAROcclusion` and `sceneSemantics` are enabled in the plugin config per PATTERNS.md.
  </action>
  <verify>
    <automated>grep "@reactvision/react-viro" apps/mobile/package.json && grep "NSCameraUsageDescription" apps/mobile/app.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains @reactvision/react-viro.
    - app.json contains the viro plugin entry with sceneSemantics and liDAROcclusion: true.
    - app.json contains NSCameraUsageDescription.
  </acceptance_criteria>
  <done>ViroReact is configured in the project metadata.</done>
</task>

<task type="auto">
  <name>Create AR Screen Skeleton & Register Navigation</name>
  <read_first>apps/mobile/src/navigation/index.tsx, apps/mobile/src/screens/HomeScreen.tsx</read_first>
  <files>apps/mobile/src/screens/ARScreen.tsx, apps/mobile/src/navigation/index.tsx, apps/mobile/src/screens/HomeScreen.tsx</files>
  <action>
    1. Create `apps/mobile/src/screens/ARScreen.tsx` following the pattern in PATTERNS.md (GameScreen analog).
    2. Implement a basic skeleton that shows an ActivityIndicator while "initializing".
    3. Add a "Start Investigation" button to `HomeScreen.tsx` that navigates to "AR".
    4. Register `ARScreen` as "AR" in `apps/mobile/src/navigation/index.tsx`.
  </action>
  <verify>
    <automated>grep "name=\"AR\"" apps/mobile/src/navigation/index.tsx && ls apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ARScreen.tsx exists and exports a default component.
    - RootNavigator includes the AR screen.
    - HomeScreen has a navigation trigger for the AR screen.
  </acceptance_criteria>
  <done>The AR screen is reachable from the home screen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Rebuild Development Client</name>
  <read_first>apps/mobile/app.json</read_first>
  <files>apps/mobile/app.json</files>
  <action>
    1. Run `npx expo prebuild` to ensure native directories are updated.
    2. Run `npx expo run:ios` or `npx expo run:android` to build the new development client.
    3. Confirm the app launches on a physical device (required for AR).
  </action>
  <what-built>Native configuration for ViroReact.</what-built>
  <how-to-verify>
    1. Run `npx expo prebuild` to ensure native directories are updated.
    2. Run `npx expo run:ios` or `npx expo run:android` to build the new development client.
    3. Confirm the app launches on a physical device (required for AR).
  </how-to-verify>
  <verify>
    <automated>echo "Human verification required for native build"</automated>
  </verify>
  <resume-signal>Type "approved" once the app successfully launches on device.</resume-signal>
  <acceptance_criteria>
    - App launches on physical device without immediate crash.
  </acceptance_criteria>
  <done>Native modules are linked and the app runs on a physical device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native -> JS | AR tracking data and camera feed status passing to React Native. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Camera Feed | mitigate | Ensure NSCameraUsageDescription is clear and only requested when AR is active. |
| T-01-02 | Tampering | AR Config | mitigate | Store AR plugin configuration in app.json (static config), reducing runtime injection risk. |
</threat_model>

<verification>
- App launches on a physical device.
- Tapping 'Start Investigation' on Home opens the AR Screen (currently showing a loader).
</verification>

<success_criteria>
- Project dependencies and native configs are set for AR.
- Navigation flow to AR mode is established.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ar-engine-foundation/01-01-SUMMARY.md`
</output>
