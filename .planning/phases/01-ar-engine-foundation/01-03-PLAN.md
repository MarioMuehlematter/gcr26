---
phase: "01-ar-engine-foundation"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02"]
files_modified: ["apps/mobile/src/screens/ARScreen.tsx", "apps/mobile/src/components/ARPlaneVisualization.tsx"]
autonomous: false
requirements: ["CORE-02"]

must_haves:
  truths:
    - "Horizontal surfaces (floors) are detected and visually marked."
    - "Vertical surfaces (walls) are detected and visually marked."
    - "Visualization uses a subtle overlay to indicate mapping progress."
  artifacts:
    - path: "apps/mobile/src/components/ARPlaneVisualization.tsx"
      provides: "Visual feedback for detected surfaces"
  key_links:
    - from: "ARScreen"
      to: "ARPlaneVisualization"
      via: "ViroARPlane selector integration"
---

<objective>
Implement plane detection and visualization. 
This provides the player with feedback on which surfaces are "known" to the engine, enabling future clue placement on floors and walls as per D-03 and D-04.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-ar-engine-foundation/01-CONTEXT.md
@.planning/phases/01-ar-engine-foundation/01-PATTERNS.md
@apps/mobile/src/screens/ARScreen.tsx

<interfaces>
From @reactvision/react-viro:
```typescript
import {
  ViroARPlane,
  ViroARPlaneSelector,
  ViroQuad,
  ViroMaterials,
} from '@reactvision/react-viro';

// Material definition
ViroMaterials.createMaterials({
  grid: {
    diffuseColor: 'rgba(0, 255, 255, 0.5)', // Cyan dot-grid placeholder
  },
});

// Plane usage
<ViroARPlane minHeight={0.5} minWidth={0.5} alignment="Horizontal">
  <ViroQuad
    rotation={[-90, 0, 0]}
    width={1}
    height={1}
    materials={["grid"]}
  />
</ViroARPlane>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Create Plane Visualization Component</name>
  <read_first>apps/mobile/src/screens/ARScreen.tsx</read_first>
  <files>apps/mobile/src/components/ARPlaneVisualization.tsx</files>
  <action>
    1. Create `ARPlaneVisualization.tsx` component.
    2. Define a `ViroMaterial` named "planeGrid" with a semi-transparent cyan color (placeholder for dot-grid texture).
    3. Implement a component that takes `alignment` ("Horizontal" or "Vertical") as a prop.
    4. Use `ViroARPlane` (or `ViroARPlaneSelector` for multi-plane selection testing) to wrap a `ViroQuad`.
    5. The `ViroQuad` should be rotated correctly (-90 on X for horizontal) and scaled to match the detected plane.
  </action>
  <verify>
    <automated>grep "ViroARPlane" apps/mobile/src/components/ARPlaneVisualization.tsx && grep "alignment" apps/mobile/src/components/ARPlaneVisualization.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component exports a visualization for detected planes.
    - Component supports both Horizontal and Vertical alignment.
    - Material "planeGrid" is defined and used.
  </acceptance_criteria>
  <done>Plane visualization component is ready.</done>
</task>

<task type="auto">
  <name>Integrate Plane Detection in AR Scene</name>
  <read_first>apps/mobile/src/screens/ARScreen.tsx, apps/mobile/src/components/ARPlaneVisualization.tsx</read_first>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    1. Import `ARPlaneVisualization` into `ARScreen.tsx`.
    2. In the `MainScene` (ViroARScene), add two instances of `ARPlaneVisualization`:
       - One for "Horizontal" (Floors).
       - One for "Vertical" (Walls).
    3. Ensure the `ViroARScene` is configured to detect both types of planes.
  </action>
  <verify>
    <automated>grep "ARPlaneVisualization" apps/mobile/src/screens/ARScreen.tsx | wc -l | grep "2"</automated>
  </verify>
  <acceptance_criteria>
    - ARScreen includes both horizontal and vertical plane visualizations.
  </acceptance_criteria>
  <done>The AR scene detects and visualizes both floors and walls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Plane Detection & Stability</name>
  <read_first>apps/mobile/src/screens/ARScreen.tsx</read_first>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    1. Open the AR screen on a physical device.
    2. Point the camera at a flat floor; verify a cyan overlay appears.
    3. Point the camera at a flat wall; verify a cyan overlay appears.
    4. Move the camera quickly and then slowly; verify the overlays stay snapped to the physical surfaces.
    5. Verify the "ORIGIN" marker remains in its starting position.
  </action>
  <what-built>Full Phase 1 AR Foundation.</what-built>
  <how-to-verify>
    1. Open the AR screen on a physical device.
    2. Point the camera at a flat floor; verify a cyan overlay appears.
    3. Point the camera at a flat wall; verify a cyan overlay appears.
    4. Move the camera quickly and then slowly; verify the overlays stay snapped to the physical surfaces.
    5. Verify the "ORIGIN" marker remains in its starting position.
  </how-to-verify>
  <verify>
    <automated>echo "Human verification required for AR tracking"</automated>
  </verify>
  <resume-signal>Type "approved" once tracking and plane detection are stable.</resume-signal>
  <acceptance_criteria>
    - Plane overlays appear on floors and walls.
    - Overlays are stable with minimal jitter.
    - Origin marker is correctly anchored.
  </acceptance_criteria>
  <done>Phase 1 goals are achieved and verified on hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment -> Engine | Physical surfaces scanned and digitized into plane coordinates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Spatial Map | accept | Plane detection only stores ephemeral bounding box data in RAM; no persistent map is saved in this phase (Phase 2 handles serialization). |
| T-01-06 | Tampering | Plane Alignment | mitigate | Use ViroReact's internal SLAM filtering to reject spurious/low-confidence plane detections. |
</threat_model>

<verification>
- Physical device test for horizontal and vertical plane detection.
- Visual confirmation of plane overlays.
</verification>

<success_criteria>
- Plane detection works for both floors and walls.
- Physical environment awareness is established.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ar-engine-foundation/01-03-SUMMARY.md`
</output>
