---
phase: "08-footstep-trails"
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["apps/mobile/src/components/FootstepBillboard.tsx", "apps/mobile/src/components/FootstepTrail.tsx"]
autonomous: true
requirements: ["PLAY-03"]

must_haves:
  truths:
    - "Footprints align with the floor surface"
    - "Footprints fade out as the player moves away (3m radius)"
    - "Trails only appear when Witcher Senses are active"
  artifacts:
    - path: "apps/mobile/src/components/FootstepBillboard.tsx"
      provides: "Individual footprint rendering"
    - path: "apps/mobile/src/components/FootstepTrail.tsx"
      provides: "Trail segment orchestration"
  key_links:
    - from: "apps/mobile/src/components/FootstepTrail.tsx"
      to: "apps/mobile/src/utils/pathing.ts"
      via: "importing generateFootprints"
---

<objective>
Implement the AR components responsible for rendering individual footprints and trail segments.

Purpose: Provide visual representation of the narrative paths in the AR scene.
Output: Two new AR components with proximity-based visibility and Witcher Sense styling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-footstep-trails/08-CONTEXT.md
@.planning/phases/08-footstep-trails/08-PATTERNS.md
@apps/mobile/src/components/ClueBillboard.tsx
@apps/mobile/src/utils/pathing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FootstepBillboard Component</name>
  <files>apps/mobile/src/components/FootstepBillboard.tsx</files>
  <action>
    - Create `FootstepBillboard.tsx` based on `ClueBillboard.tsx`.
    - It should render a `ViroQuad` flat on the ground (rotation `[-90, 0, 0]`).
    - Prop `type` determines material (`footprint_left` or `footprint_right`).
    - Materials MUST include `witcher_sense_highlight` overlay.
    - Implement Proximity Fade (D-06):
      - Take `distance` prop from `ARScreen`.
      - Opacity = `clamp(1 - (distance / 3), 0, 1)`. Footprints are invisible beyond 3 meters.
    - Component MUST NOT render if `witcherSensesActive` is false.
  </action>
  <verify>
    <automated>grep "witcher_sense_highlight" apps/mobile/src/components/FootstepBillboard.tsx</automated>
  </verify>
  <done>Individual footprints render with correct materials and proximity fade.</done>
</task>

<task type="auto">
  <name>Task 2: Create FootstepTrail Component</name>
  <files>apps/mobile/src/components/FootstepTrail.tsx</files>
  <action>
    - Create `FootstepTrail.tsx`.
    - Props: `startClue`, `endClue`, `witcherSensesActive`, `cameraPosition`.
    - Inside the component, use `useMemo` to call `generateFootprints(startClue, endClue)`.
    - Map over the generated sequence and render `FootstepBillboard` for each.
    - Calculate `distance` from `cameraPosition` to each footprint's position to pass to `FootstepBillboard`.
  </action>
  <verify>
    <automated>grep "generateFootprints" apps/mobile/src/components/FootstepTrail.tsx</automated>
  </verify>
  <done>Trail component correctly orchestrates the rendering of footprint sequences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Scene → Component | Camera position and clue data passed to renderers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Denial of Service | FootstepTrail.tsx | mitigate | Use useMemo to prevent recalculating pathing logic on every frame. |
</threat_model>

<success_criteria>
1. `FootstepBillboard` only visible when distance <= 3m and senses active.
2. `FootstepTrail` correctly iterates through pathing sequence.
3. No console errors during AR scene rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/08-footstep-trails/08-02-SUMMARY.md`
</output>
