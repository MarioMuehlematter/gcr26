---
phase: "08-footstep-trails"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["packages/shared/src/types.ts", "packages/shared/src/clues.json", "apps/mobile/src/utils/pathing.ts", "apps/mobile/src/services/mapService.ts"]
autonomous: true
requirements: ["PLAY-03"]

must_haves:
  truths:
    - "Clue interface supports path grouping"
    - "System can interpolate footprint locations between two waypoints"
    - "Footprint assets alternate between left and right"
  artifacts:
    - path: "apps/mobile/src/utils/pathing.ts"
      provides: "Stride interpolation logic"
    - path: "packages/shared/src/clues.json"
      provides: "Footprint L/R definitions"
  key_links:
    - from: "apps/mobile/src/utils/pathing.ts"
      to: "packages/shared/src/types.ts"
      via: "importing Clue type"
---

<objective>
Establish the data structure for narrative paths and implement the geometric interpolation logic for generating footprint sequences between investigation waypoints.

Purpose: Provide a mathematical foundation for rendering physical trails between clues.
Output: Updated types, new pathing utility, and updated persistence service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-footstep-trails/08-CONTEXT.md
@.planning/phases/08-footstep-trails/08-PATTERNS.md
@packages/shared/src/types.ts
@packages/shared/src/clues.json
@apps/mobile/src/services/mapService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Clue Schema & Asset Definitions</name>
  <files>packages/shared/src/types.ts, packages/shared/src/clues.json</files>
  <action>
    - Update `Clue` interface in `packages/shared/src/types.ts` to include optional `pathId: string` and `pathSequence: number` fields (per D-03).
    - Update `packages/shared/src/clues.json` to include `footprint_left` and `footprint_right` types. Both should use `footprint.png` for now (assets will be flipped/rotated in rendering).
  </action>
  <verify>
    <automated>npm run check-types --workspace=@gcr26/shared</automated>
  </verify>
  <done>Clue interface supports path metadata and new footprint types exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pathing Interpolation Utility</name>
  <files>apps/mobile/src/utils/pathing.ts</files>
  <action>
    Create `generateFootprints(startClue: Clue, endClue: Clue)` utility:
    - Calculate 3D Euclidean distance between clues.
    - Generate waypoints every 0.6 meters (D-04).
    - Calculate rotation for each footprint to face the `endClue`.
    - Alternate between `footprint_left` and `footprint_right` types (D-02).
    - Add a "wobble" (D-07): random offset of +/- 0.05m perpendicular to the path direction.
    - Return an array of objects: `{ position, rotation, type }`.
  </action>
  <verify>
    <automated>node -e "const p = require('./apps/mobile/src/utils/pathing'); console.log(p.generateFootprints({position:[0,0,0]}, {position:[0,0,3]}))"</automated>
  </verify>
  <done>Utility correctly calculates alternating, wobbled footprint sequences.</done>
</task>

<task type="auto">
  <name>Task 3: Update Persistence Service</name>
  <files>apps/mobile/src/services/mapService.ts</files>
  <action>
    - Update `syncClueToCloud` to ensure `pathId` and `pathSequence` are included in the Firestore payload if present on the clue object.
  </action>
  <verify>
    <automated>grep -n "pathId" apps/mobile/src/services/mapService.ts</automated>
  </verify>
  <done>Map service correctly persists path metadata to Firestore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Firestore | Path metadata is persisted to the cloud |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | pathing.ts | mitigate | Ensure utilities only process data for clues already present in the client session. |
</threat_model>

<success_criteria>
1. Clue interface supports path metadata.
2. Pathing utility produces a valid sequence of 5 footprints for a 3-meter path.
3. Service correctly maps new fields for cloud sync.
</success_criteria>

<output>
After completion, create `.planning/phases/08-footstep-trails/08-01-SUMMARY.md`
</output>
