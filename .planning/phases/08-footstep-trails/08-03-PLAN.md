---
phase: "08-footstep-trails"
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["apps/mobile/src/hooks/useFootstepTrails.ts", "apps/mobile/src/screens/ARScreen.tsx"]
autonomous: true
requirements: ["PLAY-03"]

must_haves:
  truths:
    - "Trail segments only appear after their origin clue is discovered"
    - "Trails are grouped by pathId and ordered by pathSequence"
  artifacts:
    - path: "apps/mobile/src/hooks/useFootstepTrails.ts"
      provides: "Segment identification logic"
  key_links:
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "apps/mobile/src/hooks/useFootstepTrails.ts"
      via: "hook usage"
---

<objective>
Integrate the footstep trail components into the investigation loop, enforcing discovery-based visibility rules and path sequencing.

Purpose: Connect narrative waypoints with visible trails based on player progression.
Output: New hook for trail management and updated ARScreen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-footstep-trails/08-CONTEXT.md
@.planning/phases/08-footstep-trails/08-PATTERNS.md
@apps/mobile/src/screens/ARScreen.tsx
@apps/mobile/src/hooks/useTeamState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useFootstepTrails Hook</name>
  <files>apps/mobile/src/hooks/useFootstepTrails.ts</files>
  <action>
    - Create `useFootstepTrails.ts`.
    - Input: `placedClues: Clue[]`, `discoveredClueIds: string[]`.
    - Logic:
      - Filter clues that have `pathId`.
      - Group clues by `pathId`.
      - Sort each group by `pathSequence`.
      - Construct "segments": pairs of consecutive clues in a path `(clue[i], clue[i+1])`.
      - Filter segments: A segment is only active if `discoveredClueIds` includes `clue[i].id` (D-05).
    - Return `activeSegments: Array<{ startClue, endClue }>`.
  </action>
  <verify>
    <automated>grep "pathId" apps/mobile/src/hooks/useFootstepTrails.ts</automated>
  </verify>
  <done>Hook correctly identifies which trail segments should be visible based on team progression.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Trails into ARScreen</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    - In `ARScreen.tsx`, call `useFootstepTrails(placedClues, discoveredClueIds)`.
    - Pass the `activeSegments` to `MainScene` via `viroAppProps`.
    - In `MainScene`, map over `activeSegments` and render `FootstepTrail` for each.
    - Pass necessary props: `startClue`, `endClue`, `witcherSensesActive`, and `cameraPosition` (derived from `cameraTransform`).
  </action>
  <verify>
    <automated>grep "FootstepTrail" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>AR Screen renders trail segments dynamically based on discovery status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Firestore → Hook | Team discovery state determines trail visibility |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | useFootstepTrails.ts | mitigate | Enforce segment visibility rule (start clue must be discovered). |
</threat_model>

<success_criteria>
1. Trails only appear for path segments where the origin has been found.
2. Trails follow the intended sequence (1 -> 2 -> 3).
3. No visual artifacts or performance drops when rendering trails.
</success_criteria>

<output>
After completion, create `.planning/phases/08-footstep-trails/08-03-SUMMARY.md`
</output>
