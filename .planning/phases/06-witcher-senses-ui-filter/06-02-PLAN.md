---
phase: 06-witcher-senses-ui-filter
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [apps/mobile/src/components/MedallionButton.tsx, apps/mobile/src/components/WitcherSenseOverlay.tsx]
autonomous: true
requirements: [PLAY-01]

must_haves:
  truths:
    - "Player can interact with a medallion button to toggle senses"
    - "Hold-to-activate logic is implemented"
    - "Visual overlay applies a darkening/grayscale effect to the screen"
  artifacts:
    - path: "apps/mobile/src/components/MedallionButton.tsx"
      provides: "Hold-to-activate interaction"
    - path: "apps/mobile/src/components/WitcherSenseOverlay.tsx"
      provides: "2D visual desaturation filter"
  key_links:
    - from: "apps/mobile/src/components/MedallionButton.tsx"
      to: "useWitcherSenses"
      via: "onPressIn/onPressOut calls to setEnabled"
---

<objective>
Create the interactive components for activating Witcher Senses and the visual overlay that simulates the "hunting mode" look.

Purpose: Provide the user interface for triggering and seeing the effect of Witcher Senses.
Output: `MedallionButton` and `WitcherSenseOverlay` components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-witcher-senses-ui-filter/06-01-SUMMARY.md
@.planning/phases/06-witcher-senses-ui-filter/06-PATTERNS.md
@apps/mobile/src/components/LockingProgressRing.tsx
@apps/mobile/src/components/RelocalizationOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MedallionButton component</name>
  <files>apps/mobile/src/components/MedallionButton.tsx</files>
  <read_first>
    - apps/mobile/src/components/LockingProgressRing.tsx (animation pattern)
  </read_first>
  <action>
    1. Create `apps/mobile/src/components/MedallionButton.tsx`.
    2. Use `TouchableOpacity` or `Pressable` with `onPressIn` and `onPressOut` (D-03).
    3. Accept an `onToggle(active: boolean)` prop.
    4. Implement a "pulse" animation when active using `Animated`.
    5. Use a placeholder SVG or Icon for the medallion (Wolf head or similar abstract shape).
    6. Style it as a large circular button at the bottom center.
  </action>
  <acceptance_criteria>
    - Button triggers `onToggle(true)` on press in and `onToggle(false)` on press out.
    - Button is styled as a medallion.
    - Pulse animation runs while pressed.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "onPressIn" apps/mobile/src/components/MedallionButton.tsx</automated>
  </verify>
  <done>MedallionButton component created.</done>
</task>

<task type="auto">
  <name>Task 2: Create WitcherSenseOverlay component</name>
  <files>apps/mobile/src/components/WitcherSenseOverlay.tsx</files>
  <read_first>
    - apps/mobile/src/components/RelocalizationOverlay.tsx (overlay pattern)
  </read_first>
  <action>
    1. Create `apps/mobile/src/components/WitcherSenseOverlay.tsx`.
    2. Implement a full-screen absolute positioned `View`.
    3. When `active` is true (prop), show:
       - A semi-transparent background `rgba(50, 50, 50, 0.4)` (D-01).
       - A subtle vignette effect (darker edges) using a circular gradient or overlapping views.
       - A 300ms fade-in/out transition using `Animated.timing`.
    4. Set `pointerEvents="none"` so it doesn't block touches to the AR scene (except the medallion button which should be ABOVE this overlay or outside it).
  </action>
  <acceptance_criteria>
    - Overlay fades in when `active` prop is true.
    - Vignette and grayscale-simulating color are applied.
    - Overlay does not block touch interactions.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "rgba(50, 50, 50, 0.4)" apps/mobile/src/components/WitcherSenseOverlay.tsx</automated>
  </verify>
  <done>WitcherSenseOverlay component created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> State | Medallion button interaction must accurately reflect in the hook state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | S | MedallionButton | accept | UI interaction spoofing is low risk for this feature. |
</threat_model>

<verification>
- Button interaction toggles the active state.
- Overlay visual appears/disappears with animation.
</verification>

<success_criteria>
- Interactive UI components are ready for screen integration.
</success_criteria>

<output>
After completion, create `.planning/phases/06-witcher-senses-ui-filter/06-02-SUMMARY.md`
</output>
