---
phase: 06-witcher-senses-ui-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/package.json, apps/mobile/src/hooks/useWitcherSenses.ts, apps/mobile/src/components/ClueBillboard.tsx]
autonomous: true
requirements: [PLAY-01]

must_haves:
  truths:
    - "A hook exists to manage Witcher Senses state (active/inactive)"
    - "Clues can render with an emissive highlight when senses are active"
  artifacts:
    - path: "apps/mobile/src/hooks/useWitcherSenses.ts"
      provides: "Witcher Senses state and haptic triggers"
    - path: "apps/mobile/src/components/ClueBillboard.tsx"
      provides: "Highlight material for active senses"
  key_links:
    - from: "apps/mobile/src/hooks/useWitcherSenses.ts"
      to: "expo-haptics"
      via: "Haptics.impactAsync"
---

<objective>
Setup the foundation for Witcher Senses, including the state management hook and the visual highlighting logic for clues.

Purpose: Provide the logic and rendering capabilities needed for the "hunting mode". Audio muffling is deferred per 06-CONTEXT.md, despite ROADMAP.md mention, prioritizing visual/haptic feedback for this phase.
Output: `useWitcherSenses` hook and updated `ClueBillboard`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-witcher-senses-ui-filter/06-CONTEXT.md
@.planning/phases/06-witcher-senses-ui-filter/06-PATTERNS.md
@apps/mobile/src/hooks/useARSession.ts
@apps/mobile/src/components/ClueBillboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useWitcherSenses hook and install dependency</name>
  <files>apps/mobile/package.json, apps/mobile/src/hooks/useWitcherSenses.ts</files>
  <read_first>
    - apps/mobile/package.json (current dependencies)
    - apps/mobile/src/hooks/useARSession.ts (hook pattern)
  </read_first>
  <action>
    1. Run `npm install expo-haptics` in `apps/mobile` directory to add the dependency.
    2. Create `apps/mobile/src/hooks/useWitcherSenses.ts` that:
       - Manages `active` boolean state.
       - Provides `setEnabled(bool)` function.
       - Triggers `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` when state changes to true (D-04).
       - Provides a `triggerDetectionHaptic()` helper for use when a clue enters the field of view or is discovered.
  </action>
  <acceptance_criteria>
    - `expo-haptics` is present in `package.json`.
    - `useWitcherSenses` hook exports `active`, `setEnabled`, and `triggerDetectionHaptic`.
    - Haptic trigger is called on activation.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "expo-haptics" apps/mobile/package.json && grep -q "useWitcherSenses" apps/mobile/src/hooks/useWitcherSenses.ts</automated>
  </verify>
  <done>Hook and dependency setup complete.</done>
</task>

<task type="auto">
  <name>Task 2: Update ClueBillboard with Witcher Sense highlight material</name>
  <files>apps/mobile/src/components/ClueBillboard.tsx</files>
  <read_first>
    - apps/mobile/src/components/ClueBillboard.tsx (current implementation)
  </read_first>
  <action>
    1. Define a new Viro material `witcher_sense_highlight` (D-02):
       - `diffuseColor: 'rgba(255, 100, 0, 0.5)'` (Orange-ish glow)
       - `lightingModel: 'Constant'`
       - `bloom: 0.5` (if supported, else just high alpha/emissive look)
    2. Add `witcherSensesActive` prop to `ClueBillboardProps`.
    3. Update `materials` array in `ClueBillboard`:
       - If `witcherSensesActive` is true, append `witcher_sense_highlight` to the materials array.
  </action>
  <acceptance_criteria>
    - `witcher_sense_highlight` material is created.
    - `ClueBillboard` accepts `witcherSensesActive` prop.
    - Highlight material is applied when prop is true.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "witcher_sense_highlight" apps/mobile/src/components/ClueBillboard.tsx</automated>
  </verify>
  <done>ClueBillboard updated with highlight logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component -> Hook | Hook state must correctly propagate to UI components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | D | useWitcherSenses | mitigate | Ensure haptic feedback doesn't cause main thread lag by using async calls. |
</threat_model>

<verification>
- Hook state changes trigger haptics.
- ClueBillboard renders highlight when prop is passed.
</verification>

<success_criteria>
- Hook and materials are ready for integration.
</success_criteria>

<output>
After completion, create `.planning/phases/06-witcher-senses-ui-filter/06-01-SUMMARY.md`
</output>
