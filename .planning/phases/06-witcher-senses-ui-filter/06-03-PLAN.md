---
phase: 06-witcher-senses-ui-filter
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [apps/mobile/src/components/HuntingHUD.tsx, apps/mobile/src/components/DiscoveryNotification.tsx, apps/mobile/src/screens/ARScreen.tsx]
autonomous: false
requirements: [PLAY-01]

must_haves:
  truths:
    - "Player sees a hunting-specific HUD when senses are active"
    - "A notification appears when a new clue is discovered"
    - "Witcher Senses are fully integrated into the main AR experience"
  artifacts:
    - path: "apps/mobile/src/components/HuntingHUD.tsx"
      provides: "Active Senses status display"
    - path: "apps/mobile/src/components/DiscoveryNotification.tsx"
      provides: "Visual feedback for new clues"
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "Main integration point"
  key_links:
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "useWitcherSenses"
      via: "Hook usage"
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "MedallionButton"
      via: "Rendering as child"
    - from: "apps/mobile/src/screens/ARScreen.tsx"
      to: "useTeamState"
      via: "Monitoring discoveredClueIds"
---

<objective>
Finalize the Witcher Senses feature by creating the specialized HUD and notification components, and integrating everything into the ARScreen.

Purpose: Deliver the complete player experience for Witcher Senses.
Output: `HuntingHUD`, `DiscoveryNotification`, and updated `ARScreen`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-witcher-senses-ui-filter/06-02-SUMMARY.md
@apps/mobile/src/screens/ARScreen.tsx
@apps/mobile/src/hooks/useTeamState.ts
@apps/mobile/src/hooks/useWitcherSenses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HuntingHUD and DiscoveryNotification</name>
  <files>apps/mobile/src/components/HuntingHUD.tsx, apps/mobile/src/components/DiscoveryNotification.tsx</files>
  <read_first>
    - apps/mobile/src/screens/ARScreen.tsx (HUD styles)
  </read_first>
  <action>
    1. Create `apps/mobile/src/components/HuntingHUD.tsx`:
       - Shows "Witcher Senses Active" label.
       - Hidden when senses are inactive (D-05).
    2. Create `apps/mobile/src/components/DiscoveryNotification.tsx`:
       - Listens for changes in `discoveredClueIds` (passed as prop).
       - Shows a sliding notification from the top: "New Clue Discovered!" (D-06).
       - Auto-hides after 3 seconds.
  </action>
  <acceptance_criteria>
    - HuntingHUD is only visible when active.
    - DiscoveryNotification appears when `discoveredClueIds` length increases.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Witcher Senses Active" apps/mobile/src/components/HuntingHUD.tsx</automated>
  </verify>
  <done>HUD and Notification components created.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Witcher Senses into ARScreen and Trigger Haptics</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <read_first>
    - apps/mobile/src/screens/ARScreen.tsx (current implementation)
    - apps/mobile/src/hooks/useWitcherSenses.ts (interface)
    - apps/mobile/src/hooks/useTeamState.ts (interface)
  </read_first>
  <action>
    1. Import `useWitcherSenses`, `useTeamState`, `MedallionButton`, `WitcherSenseOverlay`, `HuntingHUD`, and `DiscoveryNotification`.
    2. Initialize `useWitcherSenses` and `useTeamState` in `ARScreen`.
    3. Implement detection haptic trigger:
       - Use a `useEffect` that monitors `discoveredClueIds` from `useTeamState`.
       - If `discoveredClueIds.length` increases AND `active` (from `useWitcherSenses`) is true, call `triggerDetectionHaptic()` (D-04).
    4. Pass `witcherSensesActive` to `MainScene` via `viroAppProps`.
    5. In `MainScene`, pass `witcherSensesActive` to `ClueBillboard`.
    6. In `ARScreen` return:
       - Place `WitcherSenseOverlay` (absolute fill).
       - Wrap existing `HUD` with logic to hide when senses are active.
       - Render `HuntingHUD` when senses are active.
       - Render `MedallionButton` at bottom center.
       - Render `DiscoveryNotification` at top.
  </action>
  <acceptance_criteria>
    - All Witcher Sense components are rendered in ARScreen.
    - Normal HUD is hidden when senses are active.
    - Clues show highlights when senses are active.
    - Haptic detection pulse triggers when a new clue is discovered while senses are active.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useWitcherSenses" apps/mobile/src/screens/ARScreen.tsx && grep -q "useTeamState" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>Witcher Senses integrated into ARScreen with haptic detection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Witcher Senses Experience</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>Human verification of the integrated Witcher Senses experience.</action>
  <what-built>Full Witcher Senses UI and visual filter.</what-built>
  <how-to-verify>
    1. Open AR Screen.
    2. Press and hold the Medallion button at the bottom.
    3. Verify:
       - Screen turns darker/grayscale with a vignette.
       - Device provides haptic feedback on press.
       - Any visible clues glow with an orange-ish highlight.
       - HUD shows "Witcher Senses Active".
    4. While holding the button, simulate discovering a new clue (e.g., via console or another device).
    5. Verify:
       - Device provides a subtle haptic pulse (detection haptic).
       - "New Clue Discovered" notification appears at the top.
    6. Release the button and verify screen returns to normal.
  </how-to-verify>
  <verify>Manual verification successful.</verify>
  <done>approved</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AR Scene -> HUD | Scene state must sync with HUD components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | R | DiscoveryNotification | accept | Rapid fire notifications could clutter UI, but clue discovery is naturally gated by player movement. |
</threat_model>

<verification>
- Visual check of the integrated experience.
- Button hold logic verification.
- Clue highlight verification.
- Haptic detection verification.
</verification>

<success_criteria>
- Phase 6 requirements (PLAY-01) met.
</success_criteria>

<output>
After completion, create `.planning/phases/06-witcher-senses-ui-filter/06-03-SUMMARY.md`
</output>
