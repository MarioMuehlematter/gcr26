---
phase: 05-narrative-state-logic
plan: 03
type: execute
wave: 3
depends_on: [05-01]
files_modified: [apps/mobile/src/hooks/useCluePlacement.ts, apps/mobile/src/components/PrerequisitePicker.tsx, apps/mobile/src/screens/PlacementScreen.tsx]
autonomous: true
requirements: [ADM-03]
must_haves:
  truths:
    - "GMs can set a prerequisite for a clue during placement"
    - "The prerequisite picker shows existing placed clues"
    - "The prerequisite ID is persisted in Firestore"
  artifacts:
    - path: "apps/mobile/src/components/PrerequisitePicker.tsx"
      provides: "Prerequisite selection UI"
    - path: "apps/mobile/src/screens/PlacementScreen.tsx"
      provides: "Narrative sequencing integration"
  key_links:
    - from: "PlacementScreen"
      to: "PrerequisitePicker"
      via: "UI inclusion"
    - from: "PrerequisitePicker"
      to: "useCluePlacement"
      via: "addClue parameters"
---

<objective>
Provide the Game Master with the tools to define the narrative discovery chain during clue placement.

Purpose: Allow GMs to author linear storylines in AR.
Output: Prerequisite selection component integrated into the clue placement workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-narrative-state-logic/05-CONTEXT.md
@.planning/phases/05-narrative-state-logic/05-PATTERNS.md
@apps/mobile/src/screens/PlacementScreen.tsx
@apps/mobile/src/hooks/useCluePlacement.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useCluePlacement for prerequisites</name>
  <files>apps/mobile/src/hooks/useCluePlacement.ts</files>
  <action>
    Modify `useCluePlacement` to support the prerequisite ID:
    1. Update the `addClue` method to accept an optional `requiredClueId: string | null`.
    2. Add `selectedPrerequisiteId` and `setSelectedPrerequisiteId` state to the hook.
    3. Ensure the `newClue` object created in `addClue` includes the `requiredClueId` in its metadata or top-level field (match Task 1 of Plan 05-01).
  </action>
  <verify>
    <automated>grep -q "requiredClueId" apps/mobile/src/hooks/useCluePlacement.ts</automated>
  </verify>
  <done>Placement hook updated to handle narrative linking</done>
</task>

<task type="auto">
  <name>Task 2: Create PrerequisitePicker component</name>
  <files>apps/mobile/src/components/PrerequisitePicker.tsx</files>
  <action>
    Implement a UI component for picking prerequisites:
    1. The component should take `clues: Clue[]`, `value: string | null`, and `onChange: (id: string | null) => void`.
    2. Render a simple dropdown or horizontal picker showing existing clues.
    3. Include a "None" option.
    4. Follow the analog pattern from `05-PATTERNS.md` (adapted for React Native).
  </action>
  <verify>
    <automated>test -f apps/mobile/src/components/PrerequisitePicker.tsx</automated>
  </verify>
  <done>Picker component created for GM sequencing</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into PlacementScreen</name>
  <files>apps/mobile/src/screens/PlacementScreen.tsx</files>
  <action>
    Update the `PlacementScreen` to allow setting the prerequisite (D-05):
    1. Import `PrerequisitePicker`.
    2. Render `PrerequisitePicker` in the HUD area or near the clue tray.
    3. Pass the `selectedPrerequisiteId` from `useCluePlacement` to `PrerequisitePicker`.
    4. Pass the current `placedClues` to the picker.
  </action>
  <verify>
    <automated>grep -q "PrerequisitePicker" apps/mobile/src/screens/PlacementScreen.tsx</automated>
  </verify>
  <done>GM tool updated with narrative sequencing capability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GM → Placement tool | GM defines narrative rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Deadlock loops | accept | Circular dependencies (A requires B, B requires A) are possible but GM is responsible for authoring logical paths. No automatic cycle detection in v1. |
</threat_model>

<verification>
1. Verify `useCluePlacement.ts` has prerequisite state.
2. Verify `PrerequisitePicker.tsx` exists.
3. Verify `PlacementScreen.tsx` includes the picker.
</verification>

<success_criteria>
- The GM tool allows selecting a prerequisite before placing a clue.
- Placed clues correctly store their prerequisite ID in Firestore.
- The sequencing UI provides clear feedback on the current selection.
</success_criteria>

<output>
After completion, create `.planning/phases/05-narrative-state-logic/05-03-SUMMARY.md`
</output>
