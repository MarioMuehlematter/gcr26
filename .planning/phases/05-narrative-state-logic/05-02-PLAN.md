---
phase: 05-narrative-state-logic
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [apps/mobile/src/hooks/useInvestigation.ts, apps/mobile/src/screens/ARScreen.tsx, apps/mobile/src/hooks/useCluePlacement.ts]
autonomous: true
requirements: [ADM-03]
must_haves:
  truths:
    - "ARScreen displays placed clues in the scene"
    - "Clues are only visible if their prerequisites are discovered"
    - "Clicking/interacting with a clue triggers a discovery event"
    - "Discovery events are persisted to the team's progress"
  artifacts:
    - path: "apps/mobile/src/hooks/useInvestigation.ts"
      provides: "Clue discovery trigger logic"
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "Player-side clue visibility and interaction"
  key_links:
    - from: "ARScreen"
      to: "useInvestigation"
      via: "interaction callback"
    - from: "useInvestigation"
      to: "mapService.recordClueDiscovery"
      via: "service call"
---

<objective>
Implement the player-side investigation loop, where clues are discovered through interaction and visibility is controlled by team progression.

Purpose: Connect the physical AR experience to the narrative state.
Output: Clue rendering in the main AR scene with visibility logic and discovery triggers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-narrative-state-logic/05-CONTEXT.md
@.planning/phases/05-narrative-state-logic/05-PATTERNS.md
@apps/mobile/src/screens/ARScreen.tsx
@apps/mobile/src/hooks/useCluePlacement.ts
@apps/mobile/src/components/ClueBillboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useInvestigation hook</name>
  <files>apps/mobile/src/hooks/useInvestigation.ts</files>
  <action>
    Create a hook to handle discovery events:
    1. Import `useUser` and `mapService`.
    2. Implement `export function useInvestigation()`.
    3. Expose a function `discoverClue(clueId: string)` that:
       - Gets the `teamId` from `profile` (via `useUser`).
       - Calls `mapService.recordClueDiscovery(teamId, clueId)`.
       - Returns a promise that resolves when synced.
  </action>
  <verify>
    <automated>test -f apps/mobile/src/hooks/useInvestigation.ts && grep -q "discoverClue" apps/mobile/src/hooks/useInvestigation.ts</automated>
  </verify>
  <done>Hook created for triggering discovery from UI components</done>
</task>

<task type="auto">
  <name>Task 2: Integrate clues into ARScreen with visibility logic</name>
  <files>apps/mobile/src/screens/ARScreen.tsx, apps/mobile/src/hooks/useCluePlacement.ts</files>
  <action>
    Update `ARScreen` and its hooks to display clues for players (D-02):
    1. Update `useCluePlacement.ts`: Ensure it exposes the `placedClues` from Firestore.
    2. Update `ARScreen.tsx`:
       - Import `useTeamState`, `useInvestigation`, `useCluePlacement`, and `ClueBillboard`.
       - Fetch `teamId` from `useUser`.
       - Use `useTeamState(teamId)` to get `discoveredClueIds`.
       - Use `useCluePlacement()` to load and get `placedClues` for the current map.
       - Inside `MainScene`, map over `placedClues` and render `ClueBillboard` for each.
       - Implement visibility logic: `const isVisible = !clue.requiredClueId || discoveredClueIds.includes(clue.requiredClueId)`.
       - Pass `onClick={() => discoverClue(clue.id)}` to `ClueBillboard`.
    3. Note: If `ClueBillboard` doesn't have `onClick`, add it.
  </action>
  <verify>
    <automated>grep -q "useTeamState" apps/mobile/src/screens/ARScreen.tsx && grep -q "discoverClue" apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>Player scene updated to show narrative-linked clues</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player → Clue Interaction | Interaction with AR object triggers discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Discovery trigger | mitigate | Proximity checks (Phase 9) will eventually prevent discovering distant clues. |
</threat_model>

<verification>
1. Verify `useInvestigation.ts` exists.
2. Verify `ARScreen.tsx` imports the necessary hooks.
3. Verify `MainScene` renders `ClueBillboard` components with filtering logic.
</verification>

<success_criteria>
- Players can see clues anchored to physical landmarks in `ARScreen`.
- Clues that have a prerequisite are hidden until the prerequisite is discovered.
- Interacting with a clue (clicking) marks it as discovered for the whole team.
</success_criteria>

<output>
After completion, create `.planning/phases/05-narrative-state-logic/05-02-SUMMARY.md`
</output>
