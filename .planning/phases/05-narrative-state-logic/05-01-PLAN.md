---
phase: 05-narrative-state-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/shared/src/types.ts, apps/mobile/src/services/mapService.ts, apps/mobile/src/hooks/useTeamState.ts]
autonomous: true
requirements: [ADM-03]
must_haves:
  truths:
    - "Clue interface includes an optional requiredClueId field"
    - "Team interface includes a discoveredClueIds array"
    - "mapService can update a team's discoveredClueIds atomically"
    - "useTeamState provides the current team's discovery progress"
  artifacts:
    - path: "packages/shared/src/types.ts"
      provides: "Updated Clue and Team interfaces"
    - path: "apps/mobile/src/services/mapService.ts"
      provides: "recordClueDiscovery function"
    - path: "apps/mobile/src/hooks/useTeamState.ts"
      provides: "Real-time team state synchronization"
  key_links:
    - from: "apps/mobile/src/services/mapService.ts"
      to: "Firestore (teams collection)"
      via: "arrayUnion update"
---

<objective>
Establish the data foundation and service layer for narrative state progression. This includes updating shared types to support narrative linking and implementing the logic for team-based discovery tracking.

Purpose: Enable linear discovery sequences and real-time state sync between players.
Output: Updated types, new discovery service method, and a team state synchronization hook.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-narrative-state-logic/05-CONTEXT.md
@.planning/phases/05-narrative-state-logic/05-PATTERNS.md
@packages/shared/src/types.ts
@apps/mobile/src/services/mapService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared narrative types</name>
  <files>packages/shared/src/types.ts</files>
  <action>
    Modify the shared types to support narrative state tracking (D-01, D-03):
    1. Update the `Clue` interface: Add `requiredClueId?: string;` to allow linking a clue to a prerequisite.
    2. Update the `Team` interface:
       - Add `discoveredClueIds: string[];` to track progress.
       - Add `lastDiscoveryAt?: any;` (Firestore Timestamp) for temporal tracking.
    3. Ensure `packages/shared/src/index.ts` (if it exists) still exports these types.
  </action>
  <verify>
    <automated>grep -q "requiredClueId?:" packages/shared/src/types.ts && grep -q "discoveredClueIds:" packages/shared/src/types.ts</automated>
  </verify>
  <done>Clue and Team interfaces updated in shared package</done>
</task>

<task type="auto">
  <name>Task 2: Implement recordClueDiscovery service</name>
  <files>apps/mobile/src/services/mapService.ts</files>
  <action>
    Add a method to `mapService.ts` for recording a clue discovery (D-04):
    1. Import `arrayUnion`, `updateDoc`, `doc`, and `serverTimestamp` from `firebase/firestore`.
    2. Implement `export async function recordClueDiscovery(teamId: string, clueId: string): Promise<void>`.
    3. Use `updateDoc` on the `teams` collection to `arrayUnion` the `clueId` into `discoveredClueIds` and set `lastDiscoveryAt` to `serverTimestamp()`.
    4. Follow the pattern defined in 05-PATTERNS.md.
  </action>
  <verify>
    <automated>grep -q "recordClueDiscovery" apps/mobile/src/services/mapService.ts && grep -q "arrayUnion" apps/mobile/src/services/mapService.ts</automated>
  </verify>
  <done>Service method created for atomic discovery updates</done>
</task>

<task type="auto">
  <name>Task 3: Create useTeamState hook</name>
  <files>apps/mobile/src/hooks/useTeamState.ts</files>
  <action>
    Implement a hook for real-time team state synchronization (D-03):
    1. The hook should take `teamId: string | null` as a parameter.
    2. Use `onSnapshot` from Firestore to listen to the specific team document.
    3. Return `{ team, discoveredClueIds, loading }`.
    4. Follow the sync pattern from `GameScreen.tsx` described in 05-PATTERNS.md.
  </action>
  <verify>
    <automated>test -f apps/mobile/src/hooks/useTeamState.ts && grep -q "onSnapshot" apps/mobile/src/hooks/useTeamState.ts</automated>
  </verify>
  <done>Hook implemented for syncing team discovery progress</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Mobile client updates Team document discovery list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Team.discoveredClueIds | mitigate | Firestore security rules (to be updated in later phase) should prevent removing IDs once added. |
| T-05-01-02 | Information Disclosure | All Clues | accept | While client filters visibility, technically all clues are in the subcollection. This is acceptable for v1 as the UI hides them. |
</threat_model>

<verification>
1. Verify `packages/shared/src/types.ts` has new fields.
2. Verify `apps/mobile/src/services/mapService.ts` has `recordClueDiscovery`.
3. Verify `apps/mobile/src/hooks/useTeamState.ts` exists and implements `onSnapshot`.
</verification>

<success_criteria>
- Shared types reflect the narrative logic requirements.
- Team progression can be updated atomically.
- Real-time sync of team discovery state is available via a hook.
</success_criteria>

<output>
After completion, create `.planning/phases/05-narrative-state-logic/05-01-SUMMARY.md`
</output>
