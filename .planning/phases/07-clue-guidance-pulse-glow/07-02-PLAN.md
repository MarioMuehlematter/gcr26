---
phase: "07-clue-guidance-pulse-glow"
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["apps/mobile/src/components/ClueBillboard.tsx", "apps/mobile/src/screens/ARScreen.tsx"]
autonomous: true
requirements: ["PLAY-02"]

must_haves:
  truths:
    - "Next clue pulses visually when Witcher Senses are active"
    - "Pulse frequency increases as player approaches the target clue"
    - "Off-screen auras appear in the HUD when looking away from the next clue"
  artifacts:
    - path: "apps/mobile/src/components/ClueBillboard.tsx"
      provides: "AR pulse animation"
    - path: "apps/mobile/src/screens/ARScreen.tsx"
      provides: "System integration"
  key_links:
    - from: "MainScene"
      to: "useClueGuidance"
      via: "calculating isNextClue"
---

<objective>
Implement the 3D pulse animation for the target clue and integrate the guidance logic into the main AR screen.

Purpose: Finalize the visual guidance system by connecting logic to the AR scene and HUD.
Output: Animated ClueBillboard and updated ARScreen with guidance integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/mobile/src/hooks/useClueGuidance.ts
@apps/mobile/src/components/ClueGuidanceHUD.tsx
@apps/mobile/src/components/ClueBillboard.tsx
@apps/mobile/src/screens/ARScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ViroAnimations in ClueBillboard</name>
  <files>apps/mobile/src/components/ClueBillboard.tsx</files>
  <action>
    Update `ClueBillboard` to support the sinusoidal pulse animation for target clues.
    
    Implementation details:
    1. Register `ViroAnimations`: `pulse_slow`, `pulse_medium`, `pulse_fast`, `pulse_faster` with varying durations (2.0s down to 0.5s).
    2. Add `isNext` and `distance` props to `ClueBillboard`.
    3. Update the `witcher_sense_highlight` material to be emissive (if possible) or use a pulse-specific material (per D-01, D-02).
    4. Select the animation name based on `distance` if `isNext` and `witcherSensesActive` are true.
    5. Apply the selected animation to the `ViroQuad` (pulse effect: scale and opacity).
    
    Follow the Animation Pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>npm run lint apps/mobile/src/components/ClueBillboard.tsx</automated>
  </verify>
  <done>Billboard pulses when it is the next target, with frequency responding to distance.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate guidance into ARScreen</name>
  <files>apps/mobile/src/screens/ARScreen.tsx</files>
  <action>
    Wire the guidance logic and HUD into the AR investigation screen.
    
    Implementation details:
    1. Update `viroAppProps` in `ViroARSceneNavigator` to pass `cameraTransform` (position/rotation) and `nextClueId` back to the parent `ARScreen`.
    2. Use the `onTrackingUpdated` callback in `MainScene` to calculate the current camera orientation and call `useClueGuidance`.
    3. Determine the `nextClueId` in `ARScreen` and pass it to `MainScene` via `viroAppProps`.
    4. Pass `isNext={clue.id === nextClueId}` to each `ClueBillboard` rendered in `MainScene`.
    5. Render `ClueGuidanceHUD` in the `ARScreen` overlay layer, passing it the calculated `angle` and `witcherSensesActive`.
    6. Ensure guidance is only visible when `witcherSensesActive` is true.
  </action>
  <verify>
    <automated>npm run lint apps/mobile/src/screens/ARScreen.tsx</automated>
  </verify>
  <done>Investigation screen provides both AR pulse and HUD aura guidance for the next clue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AR Scene | Visual feedback occurs entirely within the client AR environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Denial of Service | ClueBillboard | mitigate | Limit animation to only the "next" clue (D-05) to prevent performance issues from too many active ViroAnimations. |
</stride_threat_register>

<verification>
1. Verify `ClueBillboard` pulses only for the correct clue ID.
2. Verify pulse speed changes as player moves closer/further.
3. Verify HUD auras appear and fade correctly based on orientation.
</verification>

<success_criteria>
- The next clue in the narrative sequence is visually distinct via pulsing.
- Off-screen cues accurately point toward the hidden clue.
- No regression in AR tracking or frame rate during pulsing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-clue-guidance-pulse-glow/07-02-SUMMARY.md`
</output>
