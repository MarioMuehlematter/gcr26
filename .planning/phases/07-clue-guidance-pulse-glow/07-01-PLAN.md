---
phase: "07-clue-guidance-pulse-glow"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["apps/mobile/src/hooks/useClueGuidance.ts", "apps/mobile/src/components/ClueGuidanceHUD.tsx"]
autonomous: true
requirements: ["PLAY-02"]

must_haves:
  truths:
    - "useClueGuidance identifies the next undiscovered clue in the sequence"
    - "useClueGuidance calculates correct relative angle and distance to the next clue"
    - "ClueGuidanceHUD renders edge auras when a target clue is off-screen"
  artifacts:
    - path: "apps/mobile/src/hooks/useClueGuidance.ts"
      provides: "Guidance calculation logic"
    - path: "apps/mobile/src/components/ClueGuidanceHUD.tsx"
      provides: "Visual off-screen indicators"
  key_links:
    - from: "useClueGuidance.ts"
      to: "useTeamState.ts"
      via: "discoveredClueIds access"
---

<objective>
Implement the core logic for identifying the "next" clue in the investigation sequence and provide the visual foundation for off-screen guidance (HUD auras).

Purpose: Establish the mathematical and logical foundation for guiding players toward the next piece of evidence.
Output: useClueGuidance hook and ClueGuidanceHUD component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/mobile/src/hooks/useTeamState.ts
@apps/mobile/src/hooks/useCluePlacement.ts
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useClueGuidance hook</name>
  <files>apps/mobile/src/hooks/useClueGuidance.ts</files>
  <action>
    Create a hook that takes `placedClues`, `discoveredClueIds`, and `cameraTransform` (position/rotation).
    
    Implementation details:
    1. Find `nextClue`: The first clue in `placedClues` where `!discoveredClueIds.includes(clue.id)` and all prerequisites (if any) are met (per D-05).
    2. Calculate `distance`: Euclidean distance between camera position and `nextClue.position` in 3D space.
    3. Calculate `angle`: The horizontal angular difference (in degrees) between the camera's forward vector and the vector pointing toward the `nextClue`.
    4. Return `{ nextClueId, distance, angle, isVisible: distance <= 15 }` (per D-03).
    
    Use the 3D Vector Distance pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>npm test apps/mobile/src/hooks/useClueGuidance.ts</automated>
  </verify>
  <done>Hook correctly identifies next clue and calculates distance/angle relative to camera.</done>
</task>

<task type="auto">
  <name>Task 2: Create ClueGuidanceHUD component</name>
  <files>apps/mobile/src/components/ClueGuidanceHUD.tsx</files>
  <action>
    Create a component that renders visual "Aura" gradients on the screen edges to guide the player (per D-04).
    
    Implementation details:
    1. Accept `active` (boolean) and `angle` (number) as props.
    2. Use `react-native-linear-gradient` (if available) or simple absolute `View` elements with opacity gradients.
    3. If `angle > 20`, show a right-side aura. If `angle < -20`, show a left-side aura.
    4. Animate aura opacity based on `abs(angle)`: brighter as the player turns toward the target (but still off-screen), fading out when the target is centered.
    5. Use `pointerEvents="none"` to ensure it doesn't block interactions.
    
    Follow the Screen-edge Gradient pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>npm run lint apps/mobile/src/components/ClueGuidanceHUD.tsx</automated>
  </verify>
  <done>Component renders edge indicators that respond to the provided angle prop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client State | Guidance logic depends on local state (clues, team discovery) which is trusted as it's synced from Firestore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | useClueGuidance | accept | Guidance points to the "next" clue which is already meant to be found; not sensitive. |
</stride_threat_register>

<verification>
1. Verify `useClueGuidance` correctly filters for the next undiscovered clue.
2. Verify `ClueGuidanceHUD` shows auras only when the target is off-center (> 20 degrees).
</verification>

<success_criteria>
- Hook successfully returns the next clue ID and its relative position.
- HUD component visually indicates the direction of the next clue when it's outside the field of view.
</success_criteria>

<output>
After completion, create `.planning/phases/07-clue-guidance-pulse-glow/07-01-SUMMARY.md`
</output>
